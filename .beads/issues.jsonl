{"_type":"issue","id":"conceptify-959.5","title":"Phase 1 QA pass: all six use cases end-to-end","description":"Final integration gate (PRD §11 M6, §3): run UC1 (explain from session), UC2 (syntax question), UC3 (codebase tour), UC4 (interrogate + apply), UC5 (ask from app), UC6 (create project in-app) end-to-end on a clean environment. Verify the NFRs that are cheap to check: cold start \u003c1s (N1), save→refresh \u003c500ms and instant comments (N2), CLI \u003c50ms warm (N3), artifact renders offline (N6). File issues for everything found; fix ship-blockers.","acceptance_criteria":"All six use cases pass on a clean run-through; NFR spot-checks recorded; ship-blockers fixed, the rest filed.","status":"open","priority":1,"issue_type":"task","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:38Z","created_by":"Chris","updated_at":"2026-07-02T05:40:38Z","labels":["checkpoint","m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-57l.4","type":"blocks","created_at":"2026-07-01T22:41:43Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:37Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-959.1","type":"blocks","created_at":"2026-07-01T22:41:39Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-959.2","type":"blocks","created_at":"2026-07-01T22:41:40Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-959.3","type":"blocks","created_at":"2026-07-01T22:41:41Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-959.4","type":"blocks","created_at":"2026-07-01T22:41:41Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.5","depends_on_id":"conceptify-b12.9","type":"blocks","created_at":"2026-07-01T22:41:42Z","created_by":"Chris","metadata":"{}"}],"dependency_count":6,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-959.1","title":"Ask from Conceptify: composer + streaming progress (FR-5.1, FR-5.2)","description":"Self-sufficient asking (PRD §7.5, UC5): 'New thread' in a project → question composer → spawns a headless agent (adapter + models.inAppAsk, cwd = project root) instructed to author per the artifact spec and publish via the CLI. While generating: streaming progress view parsing stream-json events (current activity, tool use) in the thread view (FR-2.3), swapping to the artifact viewer the moment save-artifact lands.","acceptance_criteria":"Type a question in-app → watch live agent activity → artifact appears without touching a terminal; thread status transitions generating→ready; works in an auto-created (non-repo) topic project.","status":"open","priority":1,"issue_type":"feature","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:33Z","created_by":"Chris","updated_at":"2026-07-02T05:40:33Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.1","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:33Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.1","depends_on_id":"conceptify-b12.2","type":"blocks","created_at":"2026-07-01T22:41:35Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.1","depends_on_id":"conceptify-iho","type":"blocks","created_at":"2026-07-04T16:58:06Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.1","depends_on_id":"conceptify-nsy.4","type":"blocks","created_at":"2026-07-01T22:41:35Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.9","title":"M5 checkpoint: UC4 end-to-end","description":"Integration checkpoint for M5 (PRD §11): the full crown-jewel loop on a real artifact — comment on a diagram node AND a text selection AND a direct question → Ask follow-ups → three individual sidebar answers land live → Apply one clarification to the artifact → new version renders live, comment 'applied', other comments still anchored. Exercise cancel and a failure path too.","acceptance_criteria":"UC4 demonstrated end-to-end on a real M3-generated artifact without manual intervention; cancel and failure paths behave per FR-4.8; rough edges filed as issues.","status":"open","priority":1,"issue_type":"task","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:32Z","created_by":"Chris","updated_at":"2026-07-02T05:40:32Z","labels":["checkpoint","m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.9","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:31Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.9","depends_on_id":"conceptify-b12.4","type":"blocks","created_at":"2026-07-01T22:41:32Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.9","depends_on_id":"conceptify-b12.5","type":"blocks","created_at":"2026-07-01T22:41:32Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.9","depends_on_id":"conceptify-b12.6","type":"blocks","created_at":"2026-07-01T22:41:33Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.9","depends_on_id":"conceptify-b12.7","type":"blocks","created_at":"2026-07-01T22:41:34Z","created_by":"Chris","metadata":"{}"}],"dependency_count":4,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.7","title":"Skill follow-up guidance (FR-6.4)","description":"Teach agents the follow-up contract (PRD FR-6.4): extend the skill files (the single home of authoring rules, reused by headless runs) with: how to consume get-context output; answering via resolve-comment (concise, sidebar-appropriate answers); apply mode — edit surgically or regenerate diagrams from embedded DSL sources, save a new version via save-artifact, and NEVER break existing data-cfy-ids (stability keeps other comments anchored, FR-4.4/FR-4.7).","acceptance_criteria":"A headless run driven purely by the skill + assembled prompt correctly answers comments and performs an apply without manual prompt surgery; data-cfy-id stability rule stated explicitly and observed in a real apply run.","status":"open","priority":1,"issue_type":"feature","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:30Z","created_by":"Chris","updated_at":"2026-07-02T05:40:30Z","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.7","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:30Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.7","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:29Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.7","depends_on_id":"conceptify-b12.3","type":"blocks","created_at":"2026-07-01T22:41:30Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.6","title":"Run status UI: progress, cancel, failure surfacing (FR-4.8)","description":"Visibility into in-flight runs (PRD FR-4.8): sidebar shows the active run — spinner, per-comment progress (which are answered so far), cancel button that kills the process. Failures surface the log tail inline; full runs/\u003cid\u003e.log retained and openable for debugging. Covers both answer-mode and apply-mode runs.","acceptance_criteria":"During a batch run the UI shows live per-comment progress; cancel stops the run and marks it cancelled with partial answers preserved; a deliberately-failed run (bad binary path) shows the log tail and a path to the full log.","notes":"IMPLEMENTED (high-worker; NOT closed/committed per orchestrator).\n\nRUN STATUS UI (CommentsSidebar, covers answer + apply runs): the actions row swaps to a RunStatusBlock while a run is active — spinner + mode label ('Answering follow-ups…' / 'Applying to artifact…'), per-comment progress 'n of m answered|applied' DERIVED from store comment statuses against the run's target-id set (answer: target left open; apply: target reached applied) so each comment-updated refetch advances it with no separate counter to drift, latest run-progress detail line (mono, truncated), and Cancel -\u003e appStore.cancelActiveRun() -\u003e existing cancel_run command (SIGKILL process group; partial answers preserved; authoritative 'cancelled' arrives via run-finished).\n\nSTORE SLICE (appStore): activeRun {runId, threadId, mode, targetIds|null, lastProgress} + runFailure {runId, threadId, status failed|timeout}. Fed by run-progress/run-finished listeners added centrally in events.ts. run-finished: clears activeRun, refetches comments+threads, sets runFailure for failed|timeout (uniform FR-5.3 error class; message says which). Thread switch: cleared with the viewer slice, then syncActiveRun(threadId) -\u003e NEW get_active_run command (registry liveness + DB mode via flows::active_run_summary; status always 'running' since terminal runs leave the registry before run-finished) — re-attached runs have targetIds=null (not persisted) -\u003e indeterminate spinner, documented. run-progress for the selected thread with no tracked run also triggers a re-attach (covers runs started before selection).\n\nFAILURE SURFACING: RunFailurePanel (shown when runFailure set and no active run): failure-class message, 'Show log' loads tail on demand via NEW get_run_log_tail command (#[tauri::command], commands.rs conventions: SELECT log_path FROM follow_up_runs, flows::tail_lines(path, max_lines.unwrap_or(30)); unreadable file degrades to one explanatory line so the panel ALWAYS shows log_path), scrollable \u003cpre\u003e tail + full path displayed, Dismiss. Full runs/\u003cid\u003e.log retained on disk (engine behavior, unchanged). Run history beyond latest run: out of scope per bead.\n\nTESTS/GATES: tail_lines unit test (last-n / oversize / missing file); get_active_run's domain fn (flows::active_run_summary) tested during a live run incl. other-thread None; UI states verified by type-checked build (npm run build clean) — no JS test infra in repo; live cancel/failure interaction is the b12.9 checkpoint (acceptance's 'bad binary path' scenario maps to spawn-failure -\u003e run-finished failed -\u003e panel; spawn-failure row+log behavior already covered in runs.rs tests). just check + cargo test --workspace green.\n\nFOR b12.9 (UC4 checkpoint) — things to exercise live: Ask follow-ups with 3 mixed comments -\u003e incremental sidebar answers + progress counter; artifact file untouched (same version); second click disabled + server guard; Apply on an answered comment -\u003e updating chip -\u003e new version + live viewer refresh -\u003e applied status + re-anchoring of others; cancel mid-run -\u003e cancelled + partial answers kept; failure (e.g. settings agentBinaryPath=/nonexistent) -\u003e failure panel + log tail + path. NOTE: run from 'just dev' or with conceptify installed via just install-cli — the flow hard-errors (before any run) if the CLI can't be resolved; CONCEPTIFY_CLI env var is the escape hatch.\n\nFOR b12.7 (skill follow-up reference): both prompts point agents at ~/.claude/skills/conceptify/references/follow-ups.md defensively ('if it exists, read it first'). The prompts already carry the load-bearing contract (per-comment resolve-comment; answer files = HTML fragment or markdown, no html/head/body wrapper; apply ordering all-edits -\u003e all --applied -\u003e one save-artifact; cfy-id immutability; cfy:src regeneration; cfy:version bump). follow-ups.md should DEEPEN, not contradict: answer style/tone/length, when to include code snippets, how to read anchors (verbatim snake_case inner keys), diagram re-render specifics per renderer, and that anchor.quote/cfy_id stability is the re-attachment contract (per b12.3 notes).","status":"in_progress","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:29Z","created_by":"Chris","updated_at":"2026-07-05T00:43:00Z","started_at":"2026-07-05T00:00:38Z","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.6","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:28Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.6","depends_on_id":"conceptify-b12.4","type":"blocks","created_at":"2026-07-01T22:41:29Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.5","title":"Apply to artifact: explicit second step (FR-4.7)","description":"Live artifact evolution (PRD FR-4.7, UC4): per answered comment — or batch — 'Apply to artifact' spawns a run in apply mode (models.artifactUpdate): the agent edits/regenerates the relevant artifact portions (using embedded diagram DSL sources per FR-3.4 rather than hand-editing SVG; keeping existing data-cfy-ids stable), saves a NEW version via save-artifact, the viewer refreshes live, and the comments transition to 'applied'. Re-anchoring (M4) handles remaining comments against the new version. Simple Q\u0026A never needs this mode — it exists for 'make this clearer' feedback.","acceptance_criteria":"Apply on an answered comment produces artifact v(n+1), live viewer refresh, comment status 'applied'; other comments re-anchor or get 'reference moved'; diagram-targeted apply regenerates from embedded DSL source; thread status updating→ready throughout.","notes":"IMPLEMENTED (high-worker; NOT closed/committed per orchestrator). Same file set as b12.4 (flows.rs is the shared home).\n\nAPPLY-ORDERING DECISION (the crux, per 94m.7's note): PROMPT-ENFORCED, not server-enforced. The apply prompt's contract, in order: (1) copy the artifact to a working file (never edit the app-owned file in place); (2) make ALL edits there — data-cfy-ids immutable, diagrams regenerated from the embedded \u003c!--cfy:src--\u003e DSL (never hand-edit SVG), bump \u003cmeta name=cfy:version\u003e to n+1, stay self-contained; (3) when final, for EACH target comment: resolve-comment --id \u003cid\u003e --answer-file \u003cbrief change note\u003e --applied; (4) THEN save-artifact exactly once, as the very last CLI call. The prompt EXPLAINS why (agents follow rules they understand): --applied freezes each comment at its capture version so the save's re-attachment pass migrates only untouched comments. Rejected alternative: marking targets applied server-side before/at spawn would lie in the DB if the run fails. Residual accepted imprecision: a run dying between step 3 and 4 leaves comments 'applied' whose fix never published — visible via the loud FR-4.8 failure panel; a comment's applied status is honest about agent intent, and 'do NOT save or mark applied if you could not complete the edits' is a hard rule. Misbehaving agent that saves first degrades gracefully (re-attachment processes not-yet-applied targets — noisy, never corrupting).\n\nTARGET SEMANTICS: apply_to_artifact(thread_id, comment_ids); empty ids = all status=answered comments; explicit ids must exist on the thread and be open|answered (open-\u003eapplied one-shot is legal and tested; applied -\u003e AlreadyApplied error). NoTargetComments when nothing qualifies.\n\nTHREAD STATUS (PRD §4, decided + documented in flows.rs module docs + docs/api.md): apply runs OWN 'updating' — set unconditionally after start_run succeeds (a rejected start never touches status), emit thread-updated {project_id, thread_id, status}. A watcher awaits StartedRun.finished and runs threads::transition_thread_status(updating -\u003e ready) — a NEW conditional single-statement UPDATE appended to threads.rs — so a 'ready' already set by the agent's mid-run save-artifact is never regressed (check+write atomic); emits thread-updated ready only when a row changed. Applies to ALL terminal statuses incl. cancelled; NEVER 'error' (generation-only, FR-5.3/M6). New 'thread-updated' event wired in events.ts -\u003e refetchThreads(project_id) + refetchProjects, so status chips go live (thread lists previously only refetched on thread-created).\n\nUI: per-answered-comment 'Apply to artifact' button (CommentRow footer) + header 'Apply all answered (n)' batch button; both disabled during any active run / without an artifact. Viewer refresh needs nothing new: agent's save-artifact -\u003e HTTP route -\u003e artifact-updated -\u003e existing handleArtifactUpdated path; comment applied transitions ride comment-updated.\n\nTESTS: apply_prompt_exact_for_fixture (full exact string incl. ordering section), apply_defaults_to_answered_sets_updating_then_ready (targets exclude open+applied; updating set before return + event; watcher restores ready + event; prompt contains only the answered id, '--applied', 'mark the comments applied FIRST', save-artifact line), apply_failure_restores_ready_and_never_error (exit 3 -\u003e run failed, thread ready, exactly updating+ready events), apply_validates_targets (NoTargetComments/CommentNotFound/AlreadyApplied/open-id-legal), threads.rs transition_is_conditional_on_current_status. NOT exercised: a real agent actually calling resolve-comment+save-artifact in order (needs a live HTTP server + real CLI — the CLI-\u003ePATCH-\u003ecomment-updated and save-\u003ere-attachment paths are already integration-tested in their own modules; the ordering itself is prompt text, verified by the exact-string test). Live UC4 pass = b12.9.","status":"in_progress","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:27Z","created_by":"Chris","updated_at":"2026-07-05T00:42:30Z","started_at":"2026-07-05T00:00:37Z","labels":["crown-jewel","m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.5","depends_on_id":"conceptify-94m.7","type":"blocks","created_at":"2026-07-01T22:41:28Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.5","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:27Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.5","depends_on_id":"conceptify-b12.4","type":"blocks","created_at":"2026-07-01T22:41:27Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.5","depends_on_id":"conceptify-nsy.3","type":"blocks","created_at":"2026-07-01T22:41:28Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.4","title":"Ask follow-ups: batch sidebar-first flow (FR-4.6) + concurrency guard (FR-4.9)","description":"The interrogation loop's answer half (PRD FR-4.6, UC4): 'Ask follow-ups' button gathers ALL open comments → spawns ONE headless run (adapter + models.followUp, cwd = project root) with a prompt assembled from get-context → the agent answers EACH comment individually via resolve-comment → sidebar updates live per-comment as answers land. NO artifact modification in this mode — answering and applying are deliberately separate steps. FR-4.9: enforce one follow-up run per thread at a time (button disabled + API guard while a run is active).","acceptance_criteria":"With 3 open comments (mixed text/diagram/direct), one click produces 3 individual sidebar answers landing incrementally; artifact file untouched (same version); starting a second run on the same thread is rejected while the first is active; thread status correct throughout.","notes":"IMPLEMENTED (high-worker; NOT closed/committed per orchestrator).\n\nFILES: src-tauri/src/flows.rs (NEW: flow layer — prompt assembly, child env, guards, thread-status policy; 9 tests), src-tauri/src/runs.rs (minimal engine extension: StartRun.env Vec\u003c(String,String)\u003e applied via cmd.env before spawn — the engine genuinely lacked any way to hand the child a PATH; stays policy-free, flows decide WHAT to set; module-level allow(dead_code) dropped now that flows consume the surface), src-tauri/src/commands.rs (appended ask_follow_ups/apply_to_artifact/get_active_run/get_run_log_tail wrappers + DTOs), src-tauri/src/lib.rs (mod flows + 4 command registrations), src/lib/api.ts + src/store/appStore.ts + src/lib/events.ts + src/components/{CommentsSidebar,ThreadView}.tsx, docs/api.md ('Flow commands' section + thread-updated event row).\n\nFR-4.6 FLOW: ask_follow_ups(thread_id) -\u003e context::thread_context under one lock -\u003e guards (NoArtifact / NoOpenComments) -\u003e build_answer_prompt (pure fn, exact-string unit-tested) -\u003e child_env -\u003e runs::start_run(mode=Answer, Purpose::FollowUp via RunMode.purpose(), cwd=project root via adapter template). Returns {run_id, mode, target_comment_ids} — target ids are NOT persisted; only the starting session gets per-comment progress, a re-attached session (get_active_run) shows indeterminate spinner. Answers land per-comment via the existing PATCH /comments -\u003e comment-updated -\u003e refetch path; the flow needs no completion watcher for answer mode and NEVER touches thread status (FR-5.3-lite: failed/timeout surfaces in the run UI only, no thread 'error' — reserved for generation runs/M6).\n\nFR-4.9 GUARD: engine registry reservation is authoritative (unchanged); UI half = sidebar buttons disabled while activeRun != null (+ a 'starting' latch bridging the click-\u003eactiveRun gap); a raced second start surfaces the engine's AlreadyRunning as an inline sidebar error.\n\nCHILD PATH (§5.1 GUI PATH problem) DECISION: flows resolve the conceptify CLI once per process — precedence CONCEPTIFY_CLI env override (tests/escape hatch) -\u003e sibling of current_exe (dev builds: both workspace binaries in target/\u003cprofile\u003e) -\u003e settings::resolve_agent_binary(\"conceptify\", None) i.e. cached login-shell which (the just install-cli symlink) — and prepend its parent dir to the child PATH via StartRun.env (dedup if already a component; empty inherited PATH falls back to the macOS GUI default /usr/bin:/bin:/usr/sbin:/sbin). Resolution failure aborts BEFORE any run row exists with an actionable message. Tested end-to-end: fake agent runs 'command -v conceptify' in the child and resolves the stub.\n\nANSWER PROMPT CONTRACT (what the agent sees): identity + context (project root, thread title/id, question, absolute artifact path+version), comments as pretty-JSON array {id, artifactVersion, status, anchor VERBATIM (inner keys snake_case, same passthrough contract as get-context), body, answerHtml}, exact CLI steps (mktemp -d scratch dir; one answer file + one resolve-comment per comment, resolve as soon as each answer is ready so sidebar fills incrementally), hard rules (never save-artifact, never --applied), and a defensive pointer to ~/.claude/skills/conceptify/references/follow-ups.md 'if it exists' (b12.7 writes it). serde_json Value maps alphabetize keys -\u003e deterministic prompt, exact-string tested.\n\nTESTS (flows.rs): answer_prompt_exact_for_fixture (full exact string), ask_follow_ups_spawns_answer_run_with_prompt_and_cli_on_path (prompt content, child PATH + command -v conceptify, mode/agent row, thread status untouched, zero thread-updated events), ask_follow_ups_guards_... (NoArtifact, NoOpenComments, AlreadyRunning for BOTH flows, active_run_summary through registry+DB, guard release after cancel), prepend_path unit tests. Gates: just check clean, cargo test --workspace 170+22 green (3x flake-check on flows), npm run build clean. NOT verified live (another agent owns the app) — b12.9 checkpoint does UC4 end-to-end.","status":"in_progress","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:26Z","created_by":"Chris","updated_at":"2026-07-05T00:42:03Z","started_at":"2026-07-05T00:00:37Z","labels":["crown-jewel","m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.4","depends_on_id":"conceptify-94m.6","type":"blocks","created_at":"2026-07-01T22:41:26Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.4","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:26Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.4","depends_on_id":"conceptify-b12.2","type":"blocks","created_at":"2026-07-01T22:41:25Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.4","depends_on_id":"conceptify-b12.3","type":"blocks","created_at":"2026-07-01T22:41:25Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.3","title":"get-context + resolve-comment: API + CLI (the headless agent's I/O)","description":"How headless agents read and write (PRD §5.2, §5.5): 'conceptify get-context --thread \u003cid\u003e' → thread + question + artifact path + open comments with anchors (JSON) — the run-specific context Conceptify assembles into the prompt; 'conceptify list-comments --thread \u003cid\u003e [--status open]'; 'conceptify resolve-comment --id \u003cid\u003e --answer-file \u003cpath\u003e [--applied]' → marks answered/applied, stores answer_html, updates the sidebar live via comment-resolved event. These are the contract FR-6.4 teaches.","acceptance_criteria":"get-context output contains everything an agent needs to answer comments without DB access; resolve-comment transitions status and the sidebar updates live; --applied variant sets applied status; answer file may be markdown or HTML fragment and renders correctly in the sidebar.","notes":"b12.3 implemented (medium-worker). Kept in_progress for orchestrator to close.\n\nRESOLVE-COMMENT REUSES EXISTING ENDPOINT — no new API. PATCH /api/v1/comments/:id already does everything: status advance (answered/applied), answer_html, emits comment-updated. CLI resolve-comment is a pure wrapper: reads --answer-file CLI-side and sends it VERBATIM as answer_html (default status=answered; --applied =\u003e applied, using the legal open-\u003eapplied one-shot). Answer file may be an HTML fragment OR markdown — passed through unchanged; the SIDEBAR (frontend, not mine) must render answer_html which per FR-4.5 may be markdown or HTML. Flag for frontend/b12.7.\n\nGET-CONTEXT AGGREGATION IS A REUSABLE RUST DOMAIN FN (for b12.2). New module src-tauri/src/context.rs: pub fn thread_context(conn, thread_id) -\u003e Result\u003cThreadContext, ContextError\u003e. Composes thread + project + artifacts::latest_artifact + open comments (comments::list_comments status=Open) under one conn lock. The HTTP route (GET /api/v1/threads/:id/context in server/threads_routes.rs) just maps it to conceptify_types::ThreadContextResponse. b12.2 spawner should call context::thread_context DIRECTLY for server-side prompt assembly — do NOT shell out to the get-context CLI. ContextError::ThreadNotFound =\u003e 404.\n\nNew helpers (append-only): artifacts::latest_artifact + LatestArtifact{version,file_path}; threads::get_thread_opt (pub, refactored out of get_thread) + ThreadError::ThreadNotFound; comments_routes::to_response made pub(super) so the context route serves comments identically to GET /comments. lib.rs: single additive 'mod context;'.\n\nEXACT SHAPES.\nAPI GET /api/v1/threads/:id/context (snake_case): { thread:{id,title,initial_question,status,slug}, project:{id,name,root_path}, latest_artifact:{version,file_path}|null, open_comments:[full CommentResponse] }.\nCLI get-context (camelCase top-level): { threadId,title,question,status,slug,projectId,projectName,projectRoot,artifactVersion|null,artifactPath|null,openComments:[...] }.\nCLI list-comments: bare JSON array of comments. CLI resolve-comment: {\"ok\":true,\"id\":..,\"status\":..}.\n\nANCHOR DECISION (per bead): top-level CLI keys are camelCase, but each comment's 'anchor' object is passed through VERBATIM (inner keys stay snake_case: cfy_id, quote, start, end). Documented in docs/cli.md (Anchor passthrough) and docs/api.md. b12.7 skill guidance must tell agents the anchor is read verbatim and that cfy_id/quote stability is the re-attachment contract.\n\nVERIFY: cargo test --workspace green (122 lib incl 3 new context-route harness tests: full aggregate / null-artifact / unknown-thread-404; 22 CLI incl 10 new output-shape + resolve-flag/--applied tests). Did NOT run the live app (another agent owns it) — b12.9 exercises end-to-end.\nGATE CAVEAT: workspace 'just check' (clippy -D warnings) currently FAILS on ~12 dead-code errors in src-tauri/src/settings.rs (the PARALLEL settings agent's WIP), NOT my code. Scoped clippy on conceptify-cli/-types (-D warnings) and my lib files is clean.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:25Z","created_by":"Chris","updated_at":"2026-07-04T23:40:38Z","started_at":"2026-07-04T23:24:27Z","closed_at":"2026-07-04T23:40:38Z","close_reason":"Closed","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.3","depends_on_id":"conceptify-36s.5","type":"blocks","created_at":"2026-07-01T22:41:24Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.3","depends_on_id":"conceptify-94m.2","type":"blocks","created_at":"2026-07-01T22:41:23Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.3","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:25Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.2","title":"Headless agent spawner: process mgmt, streaming, timeout, cancel, logs","description":"The engine that runs agents (PRD §5.1 agent spawner, FR-4.8/5.3 infrastructure): tokio::process::Command (NOT tauri-plugin-shell — frontend-initiated exec is disallowed, S3), kill_on_drop(true), children tracked in managed state, stdout/stderr streamed line-by-line to the frontend via Tauri events (stream-json parsed into activity events). Timeout (default 15 min, configurable) → kill + status error. Cancel = kill process group. Full transcript written to runs/\u003crun-id\u003e.log (§5.6) and FollowUpRun row maintained (§4). PATH problem: GUI apps get minimal PATH — resolve the agent binary via login-shell lookup (zsh -lc 'which claude') once and cache, or settings override (FR-7.3).","design":"PRD §5.1, §4 FollowUpRun, N4. Status machine transitions (generating/updating/error) owned here + save-artifact.","acceptance_criteria":"A run streams parsed progress events to the frontend; cancel kills the process tree promptly; timeout fires and marks the run failed; app quit reaps children (kill_on_drop); log file contains the full transcript; binary resolution works from a Finder-launched app; crashed run never corrupts thread state (N4).","notes":"Implemented as src-tauri/src/runs.rs (engine only; flows come in b12.4/b12.5/b12.6/959.1). NOT closing/committing per orchestrator.\n\nRUST API: start_run(\u0026AppHandle\u003cR\u003e, StartRun{thread_id, mode: RunMode{Answer|Apply}, prompt}) -\u003e StartedRun{run_id, thread_id, finished: oneshot::Receiver\u003cFinishedRun\u003e}; FinishedRun{run_id, thread_id, status, exit_code, log_path}. RunRegistry (Tauri managed state, app.manage in lib.rs setup): .cancel(run_id), .active_run_for_thread(thread_id); free fn runs::active_run_for_thread(app, thread_id). One #[tauri::command]: cancel_run(run_id) (registered; b12.6's cancel button entry point). No HTTP routes, no conceptify-types changes.\n\nCOMPLETION HOOK for flow beads: engine is policy-free — it never touches threads.status. Flows await StartedRun.finished (resolves AFTER the row is terminal + run-finished emitted) and apply FR-5.3 policy there (failed/timeout -\u003e thread status error, log tail from FinishedRun.log_path). UI listens to Tauri events instead.\n\nSTATUS SET (follow_up_runs.status, free-form TEXT by schema design): running -\u003e completed|failed|cancelled|timeout. timeout kept distinct from failed so UI can say why; flows treat both as the FR-5.3 error class.\n\nMODE/'ask' DECISION: DB CHECK is mode IN ('answer','apply') — 'ask' does NOT fit. Decision: in-app asks SHOULD be run rows (third mode 'ask'), but that needs a table-rebuild migration (SQLite can't alter CHECKs); filed conceptify-iho (blocks 959.1). Engine is mode-generic: adding RunMode::Ask -\u003e Purpose::InAppAsk is the whole engine change. RunMode.purpose(): Answer-\u003eFollowUp, Apply-\u003eArtifactUpdate.\n\nPROCESS TREE KILL: spawned with process_group(0) (pgid==pid); cancel/timeout do libc::kill(-pid, SIGKILL) — SIGKILL because claude's subprocesses may ignore TERM (tested with a TERM-trapping grandchild) and a headless run has nothing to flush. kill_on_drop(true) retained as app-quit backstop (direct child only — that's exactly why the group kill exists for deliberate paths). Added libc under [target.'cfg(unix)'.dependencies] (already transitive; zero cost) + tokio features process,sync.\n\nCONCURRENCY (FR-4.9): registry reservation is check+insert under one lock, done BEFORE any DB work (closes start/start TOCTOU); DB 'running' check kept as belt. Finalize order: DB terminal update -\u003e registry remove -\u003e log marker -\u003e run-finished -\u003e oneshot, so guard and DB never disagree observably.\n\nN4: two-layer supervision — inner task streams/waits; outer maps inner panic/IO-error to failed + '[run] ABNORMAL END' log marker and ALWAYS finalizes. Bounded everywhere: 5s drain grace after kill, 10s reap grace on wait() (escaped-pipe-holder can't wedge the row). Boot: runs::reconcile_stale_runs(conn) wired in lib.rs setup BEFORE registry is managed — running rows from a crashed session -\u003e failed + log marker; failure aborts startup (DB is broken anyway). Spawn failure marks the row failed (honest history) and frees the guard; missing cwd (FR-1.3 re-map case) errors cleanly before any row.\n\nEVENTS (docs/api.md 'Run events'): run-progress {run_id, thread_id, kind, detail} per stdout line (kind = stream-json type or 'output', detail = subtype or 200-char-truncated line; deliberately shallow); stderr -\u003e log only. run-finished {run_id, thread_id, status} exactly once post-terminal. Log format runs/\u003crun-id\u003e.log: [out]/[err] stream tags + [run] lifecycle markers; artifacts::run_log_path() helper appended.\n\nTESTS: 11 in runs.rs, all via fake shell-script agents through a 'fake' settings adapter (absolute command path -\u003e no which; prompt arg smuggles per-test pidfile). Cover: completed lifecycle (row/log/events/registry), nonzero exit -\u003e failed + exit_code, 1s timeout -\u003e group killed \u003c15s + status timeout, cancel kills TERM-trapping grandchild (pid liveness-checked), FR-4.9 guard (structured AlreadyRunning naming live run; releases after finish), spawn-failure row+guard, missing cwd, unknown thread, boot reconciliation (stale-\u003efailed, terminal untouched, idempotent). Gates: just check clean, cargo test --workspace 160+22 green (with parallel agents' anchoring + skill changes present).\n\nNOTE for orchestrator: settings.rs's module-level #![allow(dead_code)] can now be narrowed (spawner consumes resolve/resolve_agent_binary/Purpose), but settings.rs was out of my file boundary — one-line cleanup for whoever touches it next; runs.rs carries the same allow until the flow beads call start_run.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:24Z","created_by":"Chris","updated_at":"2026-07-05T00:00:23Z","started_at":"2026-07-04T23:41:23Z","closed_at":"2026-07-05T00:00:23Z","close_reason":"Closed","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.2","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:23Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.2","depends_on_id":"conceptify-b12.1","type":"blocks","created_at":"2026-07-01T22:41:23Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.1","title":"Agent adapter + per-purpose model settings (§5.5)","description":"The agent-agnostic invocation layer (PRD §5.5, G6, FR-7.1 backend): settings-defined command templates — adapters map name → {command, args with {prompt}/{model}/{project_root} placeholders, cwd}; defaultAdapter; per-purpose models {followUp, artifactUpdate, inAppAsk} so small sidebar answers don't burn a frontier model. Phase 1 ships and tests only the 'claude' adapter (claude -p ... --output-format stream-json --permission-mode acceptEdits) but ALL invocation flows go through the template — nothing hardcoded. Stored in SQLite settings; sensible defaults so zero config works when claude is on PATH.","design":"PRD §5.5 JSON shape, §12 OQ3 (permission scoping investigated separately). Codex adapter itself is Phase 2 — this task just guarantees the seam.","acceptance_criteria":"Adapter config round-trips through settings storage; template expansion covers prompt/model/cwd placeholders with correct shell-safe arg handling (args array, no string splitting); swapping the model per purpose changes the spawned command; a hypothetical second adapter can be added purely via config.","notes":"Implemented backend data model + resolution (src-tauri/src/settings.rs, new). NOT closing/committing per orchestrator.\n\nSTORAGE: reuses the existing M0 key/value 'settings' table (no migration added). All agent config lives under ONE key: 'agent_settings' (JSON). Defaults live in CODE (AgentSettings::default), never seeded rows -\u003e zero-config works with no row present (FR-7.4). get_settings merges a stored blob over defaults via FIELD-LEVEL serde defaults (#[serde(default=..)] per field + Default impls), so a partial/older blob still fills missing fields from code. JSON is camelCase, matching PRD §5.5: adapters{command,args,cwd}, defaultAdapter, models{followUp,artifactUpdate,inAppAsk}, plus timeoutSecs (u64 SECONDS, default 900=15min FR-5.3) and agentBinaryPath (Option\u003cString\u003e, null/empty=no override, FR-7.3). update_settings validates default_adapter names an existing adapter before writing; write is a single atomic upsert (N4-safe).\n\nRESOLUTION API for b12.2 (spawner) — two calls, in order:\n  let s = settings::get_settings(\u0026conn)?;\n  let inv: ResolvedInvocation = s.resolve(Purpose::{FollowUp|ArtifactUpdate|InAppAsk}, project_root: \u0026Path, prompt: \u0026str)?; // {program,args,cwd}\n  let program: PathBuf = settings::resolve_agent_binary(\u0026inv.program, s.agent_binary_path.as_deref())?;\n  Command::new(program).args(\u0026inv.args).current_dir(\u0026inv.cwd); timeout = Duration::from_secs(s.timeout_secs);\nresolve() is PURE (no I/O, only errors UnknownAdapter). resolve_agent_binary is the ONLY effectful fn: override wins; else absolute command returned as-is; else cached login-shell 'zsh -lc which \u003ccmd\u003e' (BINARY_CACHE OnceLock, keyed by command). Non-abs/not-found -\u003e SettingsError::BinaryNotFound. Only the (trusted, local) command name ever touches a shell (single-quoted); prompt/args NEVER do.\n\nSUBSTITUTION SAFETY: expand() is single-pass over the TEMPLATE, whole-string per-arg, never shell-interpreted. Values inserted verbatim, scanner continues in template (not the value) so a prompt containing literal {model}/{project_root} is NOT re-substituted (classic sequential-replace injection avoided). One template string -\u003e exactly one argv element; no whitespace splitting. Tested with spaces/quotes/braces/newlines/backticks/$()/;|.\n\nCOMMANDS (appended at END of commands.rs): get_agent_settings(db) -\u003e AgentSettings; set_agent_settings(app, db, settings) -\u003e () emits 'settings-changed' event. Registered in lib.rs generate_handler!. For 959.4 UI: invoke('get_agent_settings'); invoke('set_agent_settings', { settings: {...camelCase...} }) (rename_all=snake_case; DTO is AgentSettings itself, lives in settings.rs not types crate — CLI doesn't need it). Listen for 'settings-changed'.\n\nDEAD_CODE: module has #![allow(dead_code)] because the resolve/binary seam has no in-crate caller until b12.2 (app lib is a cdylib -\u003e unused pub items flagged). Every item is unit-tested. DROP the allow when b12.2 wires the spawner.\n\nGates: 'just check' clean (clippy -D warnings, whole workspace); cargo test --workspace green (conceptify_lib 122 passed incl. 19 new settings tests + real-migration integration test settings_round_trip_against_real_migrations).","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:23Z","created_by":"Chris","updated_at":"2026-07-04T23:40:37Z","started_at":"2026-07-04T23:24:26Z","closed_at":"2026-07-04T23:40:37Z","close_reason":"Closed","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12.1","depends_on_id":"conceptify-36s.3","type":"blocks","created_at":"2026-07-01T22:41:22Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.1","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:22Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.7","title":"Anchor re-attachment across versions + 'reference moved' (FR-4.4)","description":"Anchors must survive artifact updates (PRD FR-4.4): on switching or receiving a new artifact version, re-attach each comment — primary strategy: same data-cfy-id + offsets; fallback: text-quote search with prefix/suffix. If re-anchoring fails, flag the comment 'reference moved' in the sidebar — NEVER silently drop it. This is what makes the M5 apply-to-artifact loop safe: applying a clarification can't orphan the other comments invisibly.","design":"PRD FR-4.4, FR-6.4 (skill instructs agents to keep data-cfy-ids stable precisely so this succeeds).","acceptance_criteria":"Test matrix: (a) unchanged element → anchor holds; (b) element text edited but data-cfy-id kept → re-anchors via id or quote; (c) element removed → 'reference moved' badge, comment still readable and answerable; verified against real v1→v2 artifact pairs.","notes":"IMPLEMENTED (anchor re-attachment). Left in_progress, NOT closed/committed per orchestrator.\n\nFILES: src-tauri/src/anchoring.rs (NEW: DocumentIndex pure core + reattach_thread_comments orchestration); src-tauri/src/comments.rs (appended reattach_candidates + apply_reattachment helpers, bypass status machine by design); src-tauri/src/artifacts.rs (save_artifact runs re-attachment inside the SAME tx as the version insert, after the artifact row so advanced artifact_version satisfies the composite FK; SavedArtifact gains .reattached; test_conn gained comments table); src-tauri/src/server/artifacts_routes.rs (emits one comment-updated per changed comment, same payload shape as PATCH /comments); src-tauri/src/lib.rs (mod anchoring); src-tauri/src/artifact_protocol.rs (test fixture schema only: comments table, since v2 saves now read it); docs/api.md ('Re-attachment across versions (FR-4.4)' section + events/side-effect updates); src/components/{ArtifactCommentLayer,CommentsSidebar}.tsx (comment-only doc updates, zero behavior change).\n\nSEMANTICS DECIDED:\n- PARTICIPATION: comments with artifact_version \u003c new version AND status open|answered. Previously-moved ones RETRY every save (can heal). APPLIED = frozen history, never touched (the apply typically rewrote their anchored text; re-flagging would be noise; they keep their version tag forever). NULL-anchor comments advance trivially (version-agnostic).\n- SUCCESS: artifact_version advances to the new version; prior moved flag clears; anchor rewritten ONLY when primary fields were stale (start/end repaired, cfy_id re-pointed, or dropped to quote-only when the match sits outside any cfy element). quote NEVER rewritten; extra capture fields preserved verbatim. Quote-only anchors that resolve are kept as-is (primary is repaired, never invented).\n- FAILURE: anchor_state=moved, artifact_version STAYS at the last version where the anchor resolved -\u003e (version, anchor) pair remains truthful (viewer pinned to that version still highlights it; get-context lists open comments regardless of version so agents still see it).\n- ALGORITHM: (1) primary: first-in-doc-order cfy_id element + offsets slice == quote.exact (UTF-16 visible text, script/style/noscript/template excluded, EXACTLY the bridge convention, measured against \u003cbody\u003e); (2) quote search tiered: in-original-element candidates first, then document-wide; scored by exact prefix/suffix match (0-2); requires a UNIQUE best -\u003e tie = ambiguous = moved (stricter than the bridge's best-effort decoration, deliberately: persisted decision never guesses); (3) element anchors: cfy_id survives -\u003e trivial; else collapsed-text equality against id-bearing elements (textContent semantics incl script/style, matching bridge capture); multiple matches moved unless strict nesting chain (innermost wins). Corrupt/unparseable stored anchor -\u003e moved, never dropped. Duplicate ids resolve to first in doc order (mirrors querySelector).\n- EVENTS: one comment-updated per CHANGED row (unchanged still-moved rows emit nothing). events.ts already refetches on comment-updated -\u003e sidebar/badges live with zero new frontend wiring.\n- FRONTEND: no behavior change needed. computeHighlights / CommentRow.scrollable version-equality rules are now exactly right: re-attachment makes comments follow latest server-side.\n\nFOR b12.5 (apply-to-artifact): PATCH the comment to applied BEFORE saving the updated artifact. That freezes it at the version it was written against; the save's re-attachment pass then migrates/flags only the REMAINING open/answered comments -- which is precisely the FR-4.7 safety property. If you save first, the not-yet-applied comment will be re-attached (or flagged moved if the fix rewrote its text) before the PATCH lands -- harmless but noisier.\n\nVERIFY: 24 unit tests in anchoring.rs (offsets hold / stale-offset requote w repaired offsets / renamed id doc-wide / quote moved sections / drop-to-quote-only / prefix-suffix disambiguation / ambiguous-\u003emoved incl within-host tie / in-host beats elsewhere / UTF-16 units proven w astral chars / script-style exclusion / element trivial+renamed+nested-chain+parallel-ambiguous+no-quote / corrupt anchor / empty doc / dup ids). Integration: artifacts.rs save v1 -\u003e 5 comments (text/element/null/vanishing/applied) -\u003e save v2 asserts all states+versions+offset repair+reattached set excludes applied; v3 heal test; still-moved-not-rereported test. HTTP: artifacts_routes.rs asserts comment-updated payload end-to-end via MockRuntime listen_any. Gates: just check clean, cargo test --workspace 182 pass, npm run build clean.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:22Z","created_by":"Chris","updated_at":"2026-07-05T00:00:23Z","started_at":"2026-07-04T23:03:04Z","closed_at":"2026-07-05T00:00:23Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.7","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:21Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.7","depends_on_id":"conceptify-94m.6","type":"blocks","created_at":"2026-07-01T22:41:21Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.7","depends_on_id":"conceptify-nsy.3","type":"blocks","created_at":"2026-07-01T22:41:21Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.6","title":"Comments sidebar: filters, statuses, scroll-to-anchor, highlights (FR-4.5)","description":"The interrogation home base (PRD FR-4.5): sidebar listing all comments for the thread, filterable open/answered/applied, each showing anchor excerpt, body, and — once resolved — the agent's answer rendered from HTML/markdown. Clicking a comment scrolls the artifact to its anchor (via bridge); open comments show subtle persistent highlights in the artifact. Live-updates from comment events (the M5 run flow will land answers here one by one).","acceptance_criteria":"Filters work; clicking a comment scrolls the artifact to the anchor and flashes it; answer HTML renders safely in the sidebar; a comment created/resolved via API appears/updates live without refresh.","notes":"IMPLEMENTED (sidebar). Left in_progress, NOT closed/committed per orchestrator.\n\nFILES (mine only): src/components/CommentsSidebar.tsx (NEW); src/components/ThreadView.tsx (restructured body into a horizontal split + added a Comments toggle in the header); src/App.css (added .cfy-answer scoped typography for answer_html). Did NOT add any Tauri command — the sidebar reads the existing appStore.comments slice and issues bridge.scrollToAnchor; no backend change needed.\n\nLAYOUT: ThreadView body is now flex-row: [viewer area flex-1 min-w-0 (iframe/waiting/NoArtifact)] + [CommentsSidebar w-80 shrink-0, collapsible]. Sidebar renders whenever a thread is selected (NOT gated on hasArtifact) so the 94m.5 composer is visible-but-disabled during generation. Collapse: a 'Comments' toggle button in the ThreadView header (shows live open-count badge from store) + an in-sidebar collapse chevron; sidebarOpen is useState in ThreadView (persists across thread switches since ThreadView isn't keyed), default open.\n\nROWS: driven entirely by appStore.comments (same single source of truth as ArtifactCommentLayer highlights, so API/CLI comment-created/updated events land live via events.ts-\u003erefetchComments with zero new wiring). Excerpt = anchor.quote.exact for text/element-with-quote; element-without-quote falls back to cfy_id (mono); null anchor =\u003e 'Direct question'. Body shown verbatim (whitespace-pre-wrap). answer_html rendered by AnswerHtml via Preact dangerouslySetInnerHTML (== innerHTML; \u003cscript\u003e never executes; inline handlers theoretically possible, ACCEPTED per PRD §9 single-user/trusted-agent threat model — rationale in the component docstring; no DOMPurify dep by design). Status chip open/answered/applied. Filter tabs all/open/answered/applied with live per-status counts; default 'all'.\n\nSCROLL-TO-ANCHOR: click row =\u003e artifactBridge.scrollToAnchor(anchor, comment.id) (key=id so pulse lands on the live highlight decoration for open comments). Row is only clickable (role=button + Enter/Space) when anchor!=null AND artifact_version===viewerVersion AND viewerVersion!=null. null-anchor (direct) rows are non-interactive. Cross-version comments render a neutral 'v{n}' tag and are non-clickable (scroll/highlight disabled) — cross-version re-attachment is 94m.7. moved rows render an amber 'Reference moved' badge off comment.anchor_state.\n\nFOR 94m.7: the 'Reference moved' badge already renders off anchor_state==='moved' — just flip the data; no sidebar change needed. Cross-version comments are already shown-but-inert (v-tag, no scroll/highlight); when you add cross-version re-attachment, the gate to relax is CommentRow.scrollable (currently requires artifact_version===viewerVersion) and the ArtifactCommentLayer.computeHighlights version filter.\nFOR b12.4/b12.6 (M5 run UI): the sidebar is the natural host for the FR-4.6 'Ask follow-ups' button + in-flight run status (FR-4.8) — put it in CommentsSidebar's header or above the composer. Per-comment answers arriving via comment-updated already re-render live (status chip flips, AnswerHtml appears). The sidebar consumes appStore.comments only; no per-run state exists yet.\n\nVERIFY: npm run build clean (tsc typechecks all new TSX + ThreadView edits); cargo test --workspace 103 lib + 14 CLI green incl commands::tests::comment_commands_over_ipc_create_and_list (the exact IPC path listComments/createComment reuse). LIVE (isolated-HOME npm run tauri dev, port 4477): saved the skill demo-artifact v1+v2, created text-anchor+element-anchor+null-anchor comments on v1 and an element comment on v2, PATCHed the text one to answered+answer_html. DB-observable (sqlite) + GET /comments confirmed: 4 comments oldest-first, anchors stored verbatim compact-JSON, null anchor =\u003e NULL, statuses open/answered, answer_html+resolved_at set, open_comment_count=3, status filters (open=3/answered=1). This is the same list shape list_comments IPC returns (proven by the IPC test) and the same store slice ArtifactCommentLayer already consumes live. NOT exercisable headlessly (WKWebView TCC, per qxr.5): the rendered sidebar pixels, the click-\u003escrollToAnchor round-trip into the webview, and answer_html innerHTML inside WKWebView — reasoned by construction: scrollToAnchor rides the proven bridge path (same as setHighlights), the store slice is proven live in 94m.3/94m.4.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:21Z","created_by":"Chris","updated_at":"2026-07-04T23:40:05Z","started_at":"2026-07-04T23:23:19Z","closed_at":"2026-07-04T23:40:05Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.6","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:20Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.6","depends_on_id":"conceptify-94m.1","type":"blocks","created_at":"2026-07-01T22:41:20Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.6","depends_on_id":"conceptify-94m.2","type":"blocks","created_at":"2026-07-01T22:41:19Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.5","title":"Direct follow-up composer (FR-4.3)","description":"Free-text questions without a selection (PRD FR-4.3, §4): a composer at the bottom of the thread view creating a Comment with null anchor — flows through the identical sidebar/resolution machinery as anchored comments. This is deliberately NOT a chat box: it queues an open comment for the next follow-up run.","acceptance_criteria":"Typed question appears in the sidebar as an open comment with no anchor excerpt; submits on Cmd+Enter; empty submissions rejected.","notes":"IMPLEMENTED (direct follow-up composer). Left in_progress, NOT closed/committed per orchestrator.\n\nFILES (mine only): src/components/CommentComposer.tsx (NEW); mounted at the BOTTOM of CommentsSidebar (94m.6), which sits at the right of the thread view. Placement rationale: a direct follow-up creates a Comment that lands in the sidebar list directly above the box, so bottom-of-sidebar reads as 'the bottom of the thread view' (FR-2.3's viewer+sidebar+composer) and keeps the created row in view. No backend/command change — reuses api.createComment (anchor:null) + appStore.addComment, the exact path the selection popover (94m.3) uses.\n\nBEHAVIOR: free-text textarea; submits on Cmd/Ctrl+Enter (plain Enter = newline, it's a multi-line question box) and via an 'Ask' button. Empty/whitespace-only rejected (trim -\u003e disabled button + early return). On success: appStore.addComment(comment) (optimistic append + refetch reconcile + thread badge refetch) then clears+refocuses the textarea. The new comment appears in the sidebar as an OPEN row with excerpt 'Direct question' (null anchor) — flows through the identical sidebar/resolution machinery.\n\nGUARD (composite FK): the composer needs an artifact version to attach to. viewerVersion prop = ThreadView's resolvedVersion (the concrete version in the viewer; 'latest' already resolved), or null when the thread has no artifact yet. When null the textarea+button are disabled with a hint ('Waiting for an artifact…' / 'Available once the artifact is ready'); it enables the moment the first version lands (store updates live via artifact-updated). Attaches to the currently-viewed version (consistent with anchored comments; if viewing an old pinned version, the follow-up attaches to that version — same semantics as an anchored comment made while viewing history).\n\nVERIFY: npm run build clean (tsc). cargo test green (create_comment IPC covered by comment_commands_over_ipc_create_and_list). LIVE (isolated-HOME): created a null-anchor comment via the same createComment domain path the composer wraps — sqlite shows anchor=NULL, status open, and GET /comments returns it excerpt='Direct question', open_comment_count incremented. NOT exercised headlessly: the actual textarea keypress in WKWebView (TCC) — reasoned: identical create path proven live; Cmd/Ctrl+Enter handler mirrors the popover's (proven pattern).","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:20Z","created_by":"Chris","updated_at":"2026-07-04T23:40:05Z","started_at":"2026-07-04T23:23:18Z","closed_at":"2026-07-04T23:40:05Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.5","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:19Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.5","depends_on_id":"conceptify-94m.2","type":"blocks","created_at":"2026-07-01T22:41:18Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.5","depends_on_id":"conceptify-nsy.4","type":"blocks","created_at":"2026-07-01T22:41:19Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.4","title":"Diagram-element comments via data-cfy-id (FR-4.2)","description":"Comment on diagrams, not just prose (PRD FR-4.2, UC4): any data-cfy-id-bearing element (SVG nodes, edges, groups; section headings) gets a hover affordance; clicking opens the comment popover anchored to that element id. This is why FR-3.3 mandates data-cfy-id coverage — verify the pipeline works on real M3-generated artifacts.","acceptance_criteria":"Hovering a diagram node shows the affordance; click → popover → save creates an element-anchored comment; the element gets a persistent highlight; works on a real D2/DOT-generated inline SVG from an M3 artifact.","notes":"IMPLEMENTED together with 94m.3 (shared popover machinery — same ArtifactCommentLayer). Left in_progress, NOT closed/committed per orchestrator. See 94m.3 notes for the full file list, store slice, highlight management, popover UX rules, and verification.\n\n94m.4-SPECIFIC: element_click bridge messages open the SAME popover with an 'element' anchor ({v:1,type:'element',cfy_id,quote?}) — identical create/highlight/store flow as text selection, keyed off msg.type. The hover affordance (dashed outline on [data-cfy-id]) is already the bridge's job (94m.1, always-on, no toggle), so nothing to add here for the affordance. Element highlights render as an outline on the resolved element (bridge set_highlights). element popovers do NOT react to selection_cleared (they aren't tied to a live selection).\n\nLIVE-VERIFIED on a real inline SVG: the demo artifact had \u003cg data-cfy-id='fig-auth.token-service'\u003e inside an \u003csvg\u003e; an element-anchor comment created against it stored verbatim ({\"cfy_id\":\"fig-auth.token-service\",\"quote\":{\"exact\":\"Token Service\"},\"type\":\"element\",\"v\":1}), status open/anchored, and lists back. This exercises the FR-4.2 pipeline (data-cfy-id element -\u003e element anchor -\u003e comment). NOTE the SVG-text caveat from 94m.1: text highlights are never wrapped inside SVG (namespace check) — element anchors fall back to outlining the cfy_id host, which is exactly what an SVG-node comment needs.\n\nCAVEAT for a fuller M3-artifact check: I used a hand-written SVG with a data-cfy-id group (sufficient to prove the anchor pipeline). Driving a genuine D2/DOT-generated artifact end-to-end through the real webview click was not possible headlessly (WKWebView TCC). The anchor capture + storage + highlight-command path is proven; the in-webview click gesture is reasoned by construction + green build.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:18Z","created_by":"Chris","updated_at":"2026-07-04T23:23:18Z","started_at":"2026-07-04T23:03:03Z","closed_at":"2026-07-04T23:23:18Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.4","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:18Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.4","depends_on_id":"conceptify-94m.1","type":"blocks","created_at":"2026-07-01T22:41:16Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.4","depends_on_id":"conceptify-94m.2","type":"blocks","created_at":"2026-07-01T22:41:17Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.3","title":"Text-selection comments: popover + anchor capture (FR-4.1)","description":"The first interrogation gesture (PRD FR-4.1, UC4): select text in the artifact → 'Add comment' popover appears near the selection → typing + saving creates a comment with full anchor data (data-cfy-id + offsets primary, text-quote+prefix/suffix fallback) captured via the bridge. Saved comment immediately shows a subtle persistent highlight in the artifact and appears in the sidebar.","acceptance_criteria":"Select → popover → save → highlight + sidebar entry, all feeling instant (\u003c100ms perceived, N2); anchors captured correctly for selections spanning inline elements; popover dismisses cleanly on click-away/Escape.","notes":"IMPLEMENTED (94m.3 text-selection popover + anchor capture). Left in_progress, NOT closed/committed per orchestrator.\n\nFILES: src-tauri/src/commands.rs (create_comment + list_comments Tauri commands + CommentDto + IPC test comment_commands_over_ipc_create_and_list); src-tauri/src/lib.rs (registered both in invoke_handler); src/lib/api.ts (Comment type + createComment/listComments wrappers); src/store/appStore.ts (comments slice); src/lib/events.ts (comment-created/comment-updated listeners); src/components/ArtifactCommentLayer.tsx (NEW popover+highlights+bridge msgs); src/components/ThreadView.tsx (mounts the layer, keeps an iframe element ref for rect conversion). Did NOT touch conceptify-types, comments.rs, server/**, bridge.js, artifact_protocol.rs, skill/**, docs/**.\n\nCOMMANDS (frontend talks IPC not HTTP): create_comment{thread_id,artifact_version,anchor?,body}-\u003eCommentDto and list_comments{thread_id,status?}-\u003e[CommentDto]. Thin wrappers over comments::create_comment/list_comments (same domain fns behind POST/GET /api/v1/comments). Per commands.rs convention they emit NO Tauri event — the invoking shell updates its store directly. CommentDto mirrors conceptify_types::CommentResponse field-for-field (local DTO, matching sibling ProjectDto/ThreadDto pattern).\n\nSTORE SLICE (94m.5/94m.6 reuse): appStore.comments (Comment[], oldest-first, all statuses) + commentsLoading/commentsError, cleared with the viewer on thread change (folded into clearedViewer). refetchComments(threadId?) = token-guarded, no-op unless thread selected (mirrors refetchArtifactVersions). addComment(comment) = optimistic append (instant highlight, N2) + refetchComments reconcile (token bump invalidates a stale in-flight initial load) + refetchThreads (badge). selectThread triggers refetchComments. events.ts: comment-created/updated -\u003e refetchComments(thread)+refetchThreads(project), scoped (no-op unless that thread on screen); own creates emit no event so no double.\n\nHIGHLIGHTS (single source = store): ArtifactCommentLayer computes open + artifact_version===shownVersion + anchor!=null comments -\u003e setHighlights (FULL replacement) on every comments/version change AND on every bridge 'ready' (reload wipes decorations). Cross-version comments are NOT decorated on other versions (that's 94m.7). artifact_version sent = the concrete resolved viewer version ('latest' already resolved by the store).\n\nPOPOVER UX RULES (the mid-typing question, decided+noted): textarea autofocuses; dirty = trimmed body non-empty is the single gate. New selection/element_click REPLACES the popover only while !dirty (retarget an untouched popover, never eat a half-written comment). selection_cleared dismisses a SELECTION popover only while !dirty; element popovers ignore selection_cleared. Escape + click-away (window mousedown capture, outside the popover box) ALWAYS cancel (explicit gestures). Cmd/Ctrl+Enter saves (aligns with 94m.5 composer). On save: create -\u003e appStore.addComment -\u003e popover closed (clears shell pending-selection state) -\u003e highlight appears same tick via the store-\u003esetHighlights effect. No 'clear browser selection' command exists (protocol frozen) so the ephemeral selection just clears on next click; acceptable.\n\nLIMITATION (frozen protocol): no artifact-scroll message exists, so an open popover does not track artifact scroll (element popover can drift if you scroll before saving). Popover is ephemeral; acceptable. Rects converted iframe-viewport-\u003eshell by adding iframe.getBoundingClientRect(); popover is position:fixed, placed below the target (flips above on overflow), clamped to viewport.\n\nGUARD: layer mounts only in ThreadView's hasArtifact branch, so a generating thread with no artifact can't comment (backend composite FK would 404 anyway).\n\nVERIFICATION: npm run build clean; just check clean (clippy -D warnings); cargo test --workspace 100 lib +14 CLI pass incl new IPC test (drives create_comment/list_comments over real Tauri IPC exactly as the shell invokes, asserts open/anchored + verbatim anchor round-trip + status filter). LIVE (isolated-HOME debug app, port 4477): saved a demo artifact (data-cfy-id heading/para/SVG group) via CLI, created a text-anchor (FR-4.1) and element-anchor (FR-4.2) comment through the SAME domain path my commands wrap; sqlite shows both anchors stored verbatim compact-JSON, status open/anchored, open_comment_count=2, GET list returns both oldest-first, status=applied filter empty. NOT exercised: the WKWebView popover click itself (TCC: no screenshot/inject) and emit-\u003eJS-listen in webview (qxr.5 note) — reasoned by construction + green build; the create/list/anchor path is proven live end-to-end.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:17Z","created_by":"Chris","updated_at":"2026-07-04T23:23:17Z","started_at":"2026-07-04T23:03:03Z","closed_at":"2026-07-04T23:23:17Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.3","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:17Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.3","depends_on_id":"conceptify-94m.1","type":"blocks","created_at":"2026-07-01T22:41:15Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.3","depends_on_id":"conceptify-94m.2","type":"blocks","created_at":"2026-07-01T22:41:16Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.2","title":"Comments backend: CRUD API + anchor model (FR-4.4)","description":"Comment storage and API (PRD §7.4, §4): create/list/update comments with the FR-4.4 anchoring model stored as JSON — primary anchor = data-cfy-id + character offsets within that element; fallback = text-quote with prefix/suffix (W3C Web Annotation style) for re-attachment after artifact updates. Statuses open | answered | applied; answer_html field for agent resolutions; null anchor = direct follow-up question (§4). Mutations emit Tauri events for live sidebar updates. Endpoints under /api/v1/ (list-comments with --status filter serves both the UI and the M5 CLI).","design":"PRD FR-4.4, §4 Comment entity. Anchor JSON schema documented in the API reference doc — the bridge, re-anchoring, and headless agents all consume it.","acceptance_criteria":"Comments persist with both primary and fallback anchor data; null-anchor comments accepted; status transitions enforced (open→answered→applied); list filters by status; comment mutations emit events.","notes":"IMPLEMENTATION (94m.2 comments backend — CRUD + anchor model). Left in_progress, NOT closed/committed per orchestrator.\n\nANCHOR JSON SCHEMA (FR-4.4) — canonical def in crates/conceptify-types (Anchor/TextAnchor/ElementAnchor/TextQuote), documented in docs/api.md 'Comments \u003e anchor model'. snake_case throughout (rest of the API is snake_case; the JS bridge must emit snake_case too). Envelope: every anchor is a JSON object with integer 'v' (currently 1) and a 'type' discriminator.\n  - type='text' (FR-4.1 selection): primary = cfy_id (nearest ancestor data-cfy-id) + start/end char offsets within that element's normalized textContent; fallback = quote{exact, prefix?, suffix?} (W3C text-quote). cfy_id/start/end are OPTIONAL — omitted when selection has no id-bearing ancestor (quote is then sole anchor). quote REQUIRED.\n  - type='element' (FR-4.2 diagram/heading click): primary = cfy_id (REQUIRED); quote OPTIONAL fallback (omit for graphical nodes w/o text).\n  - null anchor (field absent) = direct follow-up question (FR-4.3).\n  Server validates the ENVELOPE only (object, v==1, known type, required fields present) then stores the object VERBATIM (compact JSON) — unknown extra fields are TOLERATED and survive round-trip, so 94m.1/94m.3-4 bridge can add capture hints (e.g. captured_rect) without a server change. Unsupported v / unknown type / not-object / missing required field -\u003e 400 InvalidAnchor.\n\nANCHOR_STATE: added column via APPENDED migration #8 COMMENT_ANCHOR_STATE ('ALTER TABLE comments ADD COLUMN anchor_state TEXT NOT NULL DEFAULT ''anchored'' CHECK (anchor_state IN (''anchored'',''moved''))'). Chose a first-class column, NOT a field inside the anchor blob: the anchor JSON is authoring-time truth (immutable, opaque, stored verbatim); whether it still resolves is mutable runtime state, so a plain UPDATE avoids read-parse-rewrite and the sidebar can badge off a queryable field. 94m.7 OWNS the POLICY of when to flip anchored-\u003emoved; this bead only stores/serves it + provides the write path (PATCH anchor_state). null-anchor comments stay 'anchored' forever.\n\nSTATUS MACHINE: open|answered|applied, MONOTONIC — status may only advance or stay (rank open\u003canswered\u003capplied), regression rejected 409 with structured {error, from, to}. open-\u003eapplied DIRECTLY ALLOWED (decided per FR-4.7 + M5 resolve-comment --applied one-shot: apply-mode can resolve-with-update a never-answered comment). resolved_at stamped (DB clock, in SQL) the FIRST time a comment leaves open, stable thereafter.\n\nENDPOINTS (flat, mirroring threads):\n  - POST /api/v1/comments  body {thread_id, artifact_version, anchor:JSON|null, body}. Requires thread + (thread,artifact_version) to exist (explicit checks -\u003e clean 404 instead of opaque composite-FK error). Empty/whitespace body -\u003e 400. -\u003e 200 CommentResponse.\n  - GET  /api/v1/comments?thread_id=\u003cid\u003e[\u0026status=open|answered|applied]  oldest-first. Unknown thread -\u003e empty array (no 404, matches list_threads). Invalid status -\u003e 400. Serves UI + M5 list-comments.\n  - PATCH /api/v1/comments/{id}  body any subset of {status, answer_html, anchor_state}; \u003e=1 required else 400. Drives M5 resolve-comment. -\u003e 200 full updated CommentResponse.\n  NO delete (PRD doesn't need it Phase 1).\n\nEVENTS: comment-created {project_id, thread_id, comment_id} on POST; comment-updated {project_id, thread_id, comment_id, status} on PATCH. DECISION: named it 'comment-updated' (not the PRD's sketch 'comment-resolved') — one event covers ALL comment mutations (status, answer_html landing, anchor_state 'reference moved' flip), consistent with artifact-updated/thread-created. b12.3 (resolve-comment) MUST listen for 'comment-updated', NOT 'comment-resolved'. Documented in docs/api.md Events table. Payload carries project_id+thread_id so frontend scopes refetch.\n\nFK NOTE for 94m.1/94m.3-5: a comment can only be created against an artifact version that already exists (composite FK comments(thread_id,artifact_version)-\u003eartifacts(thread_id,version)). So the bridge must send the CURRENT rendered artifact_version. A thread still 'generating' with no artifact -\u003e ArtifactVersionNotFound 404.\n\nFILES: crates/conceptify-types/src/lib.rs (Anchor/TextAnchor/ElementAnchor/TextQuote + CreateCommentRequest/CommentResponse/ListCommentsResponse/UpdateCommentRequest; anchor carried as serde_json::Value opaque); src-tauri/src/comments.rs (new domain module, 19 unit tests); src-tauri/src/server/comments_routes.rs (new, 4 MockRuntime HTTP-harness tests incl listen_any event capture); src-tauri/src/db/migrations.rs (COMMENT_ANCHOR_STATE append); server/mod.rs + routes.rs (wiring); lib.rs (mod comments + comments_crud_against_real_migrations integration test proving anchor_state column + CHECK + composite FK live); docs/api.md (Comments section: anchor schema, status machine, 3 endpoints, Events table). GATES: just check clean (clippy -D warnings), cargo test --workspace 98+10 pass. New files rustfmt-clean.\n\nDID NOT TOUCH: frontend src/**, CLI, skill/**, justfile, docs/cli.md, docs/artifact-spec.md. Did NOT implement re-attachment (94m.7).","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:16Z","created_by":"Chris","updated_at":"2026-07-04T22:10:28Z","started_at":"2026-07-04T21:52:41Z","closed_at":"2026-07-04T22:10:28Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.2","depends_on_id":"conceptify-36s.3","type":"blocks","created_at":"2026-07-01T22:41:14Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.2","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:16Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.2","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:41:14Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"conceptify-94m.1","title":"Bridge script + injection: the in-artifact interaction layer","description":"The postMessage bridge (PRD §5.4): replace the M2 no-op stub with the real injected script providing (a) text-selection reporting — ranges/quotes to the shell for the comment popover; (b) click-to-comment on any data-cfy-id-bearing element with hover affordance; (c) rendering highlight decorations for existing comment anchors; (d) scroll-to-anchor on request from the sidebar. Shell side: validate event.source === iframe.contentWindow on every message (opaque-origin frames report origin 'null'); define a versioned message protocol. Injection stays server-side in the protocol handler so on-disk files remain pristine.","design":"PRD §5.4 bridge protocol, §9 S2. Keep the protocol small and versioned — M4/M5 features all ride on it.","acceptance_criteria":"From inside a real artifact: selecting text reports quote+range to the shell; clicking a data-cfy-id element reports its id; the shell can command a highlight decoration and a smooth scroll-to-anchor; messages from any other source are ignored; on-disk artifact unchanged.","notes":"Bridge implemented (94m.1). Decisions for downstream beads (94m.3/4/5/6/7):\n\nPROTOCOL (docs/api.md 'Bridge protocol' is the contract). Envelope {cfy:1, type,...}. Artifact-\u003eshell: ready | selection {anchor,rect} | selection_cleared | element_click {anchor,rect}. Shell-\u003eartifact: set_highlights {highlights:[{key,anchor}]} (FULL replacement; [] clears) | scroll_to_anchor {anchor, key?}. Unknown types dropped both sides (shell logs at debug).\n\nOFFSET CONVENTION (94m.7 must match): text-anchor start/end = UTF-16 code-unit offsets into the element's VISIBLE TEXT = concatenation of Text node data in doc order EXCLUDING script/style/noscript/template subtrees, NO whitespace normalization. This is the precise meaning of api.md's 'normalized text content' (clarifying pointer added there). Rationale: offsets stay stable against inline-JS/CSS churn and against the injected bridge itself (verified in harness: suffix context skips artifact \u003cscript\u003e text).\n\nQUOTES: text-anchor quotes are raw visible-text slices; prefix/suffix = up to 32 chars of DOCUMENT-wide visible text (omitted when empty). Element-anchor quotes are whitespace-collapsed+trimmed element text, omitted if empty or \u003e300 chars (re-attachment hint only, not exact-searchable).\n\nRECTS (94m.3): {x,y,width,height} in IFRAME-VIEWPORT CSS px (getBoundingClientRect at send time). Popover position = iframe element's own bounding rect + msg rect. Rects go stale on artifact scroll -\u003e dismiss/re-request on scroll or selection_cleared.\n\nHOVER AFFORDANCE DECISION: always-on subtle dashed outline on [data-cfy-id] hover; NO commenting-enabled toggle message in v1 (add a set_mode message later if ever needed). All injected style is :where()-wrapped specificity-0 (incl. :hover INSIDE :where — outside it out-ranks the highlight rules; found+fixed in harness).\n\nLIFECYCLE (94m.6): 'ready' fires after EVERY document load incl. version-switch reloads, which wipe decorations -\u003e consumers must re-send set_highlights on every ready. bridge.ts queues commands sent before the first ready of an attachment. attach() is idempotent per element; ThreadView registers via a stable useCallback ref.\n\nELEMENT_CLICK suppression: clicks that end a non-collapsed selection, and clicks on a[href]/button/form controls/summary/label/[contenteditable], do not fire.\n\nDECORATIONS: element -\u003e data-cfy-hl='element' attr (outline); text -\u003e inline span[data-cfy-hl='text'] wraps via splitText, fully unwrapped + Node.normalize() on clear (harness-verified textContent byte-identical after clear). Text resolution: offsets (verified against quote) -\u003e doc-wide quote search (prefix/suffix scored) -\u003e outline cfy_id host as coarse fallback; unresolvable skipped ('moved' flagging is 94m.7's job). SVG text nodes are never wrapped (namespace check) — falls back to element outline.\n\nTRUST: artifact JS CAN spoof artifact-\u003eshell messages (accepted, S2 containment; shell shape-validates everything); it CANNOT spoof shell-\u003eartifact commands (event.source !== window.parent; harness-verified). Both directions post with targetOrigin '*' (opaque origin unnameable); nothing sensitive rides the channel.\n\nASSET: script lives at src-tauri/assets/bridge.js, inlined via concat!(include_str!) into artifact_protocol::BRIDGE_TAG (marker data-cfy-bridge=\"v1\"); a test guards that the JS contains no literal script-tag open/close and references all six protocol message names.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:15Z","created_by":"Chris","updated_at":"2026-07-04T23:02:51Z","started_at":"2026-07-04T22:37:03Z","closed_at":"2026-07-04T23:02:51Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m.1","depends_on_id":"conceptify-94m","type":"parent-child","created_at":"2026-07-01T22:40:15Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-94m.1","depends_on_id":"conceptify-nsy.2","type":"blocks","created_at":"2026-07-01T22:41:13Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.4","title":"M3 checkpoint: quality iteration on ≥3 real repos (UC1/UC2/UC3)","description":"Integration checkpoint for M3 (PRD §11): iterate the skill on real questions across at least 3 real repos until artifact quality is consistently good — exercising UC1 (explain a subsystem from a session), UC2 (unfamiliar-syntax micro-tutorial), UC3 (whole-codebase orientation tour). Each iteration feeds fixes back into the skill authoring guide / scaffold / spec. Done means: start-to-finish from a COLD app, artifacts consistently beautiful and correct.","acceptance_criteria":"UC1, UC2, UC3 each demonstrated end-to-end from a cold app on real repos; recurring quality failures have corresponding skill-guide fixes; remaining rough edges filed as issues.","status":"in_progress","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:12Z","created_by":"Chris","updated_at":"2026-07-04T23:40:06Z","started_at":"2026-07-04T23:40:06Z","labels":["checkpoint","m3","phase1"],"dependencies":[{"issue_id":"conceptify-57l.4","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:11Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.4","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:09Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.4","depends_on_id":"conceptify-57l.2","type":"blocks","created_at":"2026-07-01T22:41:09Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.4","depends_on_id":"conceptify-57l.3","type":"blocks","created_at":"2026-07-01T22:41:10Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.4","depends_on_id":"conceptify-nsy.7","type":"blocks","created_at":"2026-07-01T22:41:11Z","created_by":"Chris","metadata":"{}"}],"dependency_count":4,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.1","title":"Author Claude Code skill v1: flow + authoring guide + visual strategy","description":"The product's other half (PRD §7.6): the skill at ~/.claude/skills/conceptify/ (source lives in this repo; installed globally). FR-6.1 trigger \u0026 flow: on 'use conceptify to explain X' — check CLI availability → ensure-project --dir \u003crepo root\u003e → create-thread → author artifact → save-artifact (app launch is free via the CLI contract). FR-6.2 authoring guide: embed the artifact spec (§7.3), the design-system scaffold template + component classes (from M2), explanation-quality guidance (hook → mental model → visuals → walkthrough → summary; visuals wherever they beat prose), and the §8 tiered visual strategy with EXACT generation-time render commands (d2 --layout=elk, dot -Tsvg, Shiki via node). Must instruct: never commit .conceptify/, suggest global gitignore.","design":"PRD §7.6, §8, §5.5 surface 1. Authoring rules live in exactly one place — the skill files — reused verbatim by headless runs in M5.","acceptance_criteria":"In a real repo with the skill installed, 'use conceptify to explain \u003ctopic\u003e' produces a spec-compliant artifact visible in the app with zero manual steps; the skill file embeds (not paraphrases) the current scaffold + spec; render commands verified to work as written.","notes":"Skill v1 decisions (57l.1):\n1. LAYOUT: skill/ is the source of truth — SKILL.md (flow FR-6.1 + authoring core FR-6.2 + WKWebView constraints), references/rendering.md (tiered visual strategy + verified render commands + SVG post-processing), scripts/highlight.mjs (Shiki helper), plus the existing design-system.css/.md and examples/demo-artifact.html. 'just install-skill' snapshots skill/ AND docs/artifact-spec.md into ~/.claude/skills/conceptify/ (copy, not symlink — install is an explicit snapshot step; repo files stay canonical, nothing hand-duplicated into prose).\n2. D5 REFINEMENT (supersedes the sentinel-hex idea for d2/dot): d2 0.7.1 emits stable theme classes (fill-B6, stroke-B1, .connection) and graphviz 15.1.0 emits class=node/edge/cluster; artifact CSS class rules beat SVG presentation attributes, so theming is done by pasting per-tool 'adapter CSS' into the artifact's second \u003cstyle\u003e block — no hex rewriting. Post-processing on the SVG itself: strip prolog + d2's embedded \u003cstyle\u003e blocks (base64 fonts + baked palette, ~15KB/diagram), strip graphviz positional id= attrs, stamp data-cfy-id (d2: base64-decode the \u003cg\u003e class = HTML-escaped DSL key, e.g. 'KGNsaWVudCAtJmd0OyBtaWRkbGV3YXJlKVswXQ==' -\u003e '(client -\u0026gt; middleware)[0]'; dot: child \u003ctitle\u003e). All verified empirically.\n3. SHIKI: shiki has no CLI and npx can't resolve ESM libs, so the skill ships scripts/highlight.mjs which bootstraps shiki@^4 into ~/.cache/conceptify/shiki-env (npm --prefix) and imports it via a shim module written into that dir (ESM resolution is relative to the importing file). vitesse-light/vitesse-dark dual themes, --highlight line ranges via transformer. Survives skill reinstall (cache lives outside ~/.claude/skills).\n4. TOOLCHAIN: d2 0.7.1 + graphviz 15.1.0 installed via brew on this machine (were missing); node 22 present. All documented commands run as written: 'd2 --layout=elk --pad 0', 'dot -Tsvg', highlight.mjs.\n5. E2E VERIFIED (isolated HOME, app built from HEAD ca59e50 in a temp worktree): ensure-project on this repo -\u003e create-thread -\u003e authored a genuine artifact (launch-and-wait contract; d2 diagram with 14 stamped anchors + 2 Shiki listings + full component usage) following SKILL.md verbatim -\u003e save-artifact returned {version:1, warningsCount:0}, thread status ready, artifact.v1.html + artifact.html in central storage. Rendering visually verified in Chrome light AND dark (agent-browser screenshots); in-app pixel check blocked by missing screen-recording permission, but the viewer serves the same validated bytes and focus-after-save succeeded.\n6. AUTHORING LESSON folded into rendering.md: long direction:right d2 chains render small at column width — prefer direction:down for 6+ node flows.\n7. SLOTS for later beads: SKILL.md step 5 is the self-review insertion point (57l.2); scope note reserves references/follow-ups.md for FR-6.4 (b12.7); doctor (57l.3) referenced in rendering.md tool-availability section.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:08Z","created_by":"Chris","updated_at":"2026-07-04T22:01:42Z","started_at":"2026-07-04T21:34:32Z","closed_at":"2026-07-04T22:01:42Z","close_reason":"Closed","labels":["m3","phase1"],"dependencies":[{"issue_id":"conceptify-57l.1","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:08Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.1","depends_on_id":"conceptify-nsy.1","type":"blocks","created_at":"2026-07-01T22:41:04Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.1","depends_on_id":"conceptify-nsy.5","type":"blocks","created_at":"2026-07-01T22:41:05Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.1","depends_on_id":"conceptify-nsy.6","type":"blocks","created_at":"2026-07-01T22:41:06Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.1","depends_on_id":"conceptify-qxr.3","type":"blocks","created_at":"2026-07-01T22:41:06Z","created_by":"Chris","metadata":"{}"}],"dependency_count":4,"dependent_count":7,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.7","title":"M2 checkpoint: hand-written test artifact renders beautifully everywhere","description":"Integration checkpoint for M2 (PRD §11): hand-write a rich test artifact (exercising the full design system, inline SVG diagrams with data-cfy-id, Shiki-rendered code, metadata block — a real explanation, not lorem ipsum) and verify it renders beautifully in-app through the full pipeline (CLI save-artifact → artifact:// → sandboxed iframe) AND standalone in Safari and Chrome. Fix whatever this shakes out; keep the artifact in-repo as a golden test fixture for viewer/validator regressions.","acceptance_criteria":"Same .html file: gorgeous in-app, in Safari, and in Chrome; light and dark; offline; checked into the repo as a fixture; any pipeline bugs found are fixed or filed.","notes":"M2 checkpoint verification pass (nsy.7). Ran the app from a HEAD (cf93696) worktree via 'npm run tauri dev' under an isolated HOME (real DB untouched, mtime unchanged; CARGO_HOME/RUSTUP_HOME pinned to the real home so the toolchain resolved). Drove the REAL CLI against it: status -\u003e ensure-project -\u003e create-thread -\u003e save-artifact skill/examples/demo-artifact.html =\u003e version 1, warningsCount 0, thread status 'ready', window auto-focused. Second save =\u003e version 2 (follow_up), v1 file immutable (sha unchanged), and the CLI surfaced W-VERSION-MISMATCH (warningsCount 1) — validator warning path confirmed end-to-end. Byte-identity: stored artifact.v1.html == artifact.html == input (sha256 43728510...), so the in-app iframe serves exactly the file I verified in browsers.\n\nVISUAL RESULTS (gorgeous in every case: editorial palette, serif display, dual-theme Shiki code swaps correctly, SVG diagrams themed, callouts/steps/table/details all correct):\n- Chrome (agent-browser, real Blink): light + dark + offline, full-page. HAR shows ONLY the document load — zero external requests; explicit 'set offline on' reload renders identically. Artifact audited: no \u003cscript\u003e/\u003clink\u003e/remote fonts; the only external refs are text inside a Shiki code block and one click-only \u003ca href\u003e to MDN.\n- Safari / in-app WKWebView (real WebKit, offscreen WKWebView.takeSnapshot — same engine as the app iframe AND Safari): light + dark. In-app proxy rendered the EXACT served bytes (file + injected bridge stub) — stub doesn't affect layout. Widths 288/640/900.\n\nCAPTURED vs PROXIED: screencapture is blocked (screen-recording permission denied: 'could not create image from display'), so the actual app window and Safari window could not be pixel-captured. In-app pixels are PROXIED via a real WKWebView snapshot of the served bytes (identical engine + identical bytes); the pipeline itself was verified FUNCTIONALLY (save-\u003eready-\u003efocus, deterministic iframe src artifact://localhost/\u003cthread\u003e/\u003cn\u003e, protocol handler unit-tested). Safari.app opened the file (renders fine) but AppleScript control hangs on the TCC automation prompt.\n\nBUG FOUND -\u003e filed conceptify-nsy.8 (P2): inline \u003ccode\u003e overflows horizontally below ~458px pane width (overflow-wrap:normal on a 42-char unbreakable token). Not fixed here: design-system.css is a closed nsy.5 deliverable + a parallel agent was active, and acceptance permits filing; the demo embeds the scaffold verbatim so both must change together. Everything else (pre/tables/SVGs) scales/clips correctly; at \u003e=460px pane the artifact is flawless.\n\nFIXTURE DECISION: keep the golden fixture at skill/examples/demo-artifact.html (single source of truth; already referenced by 'just install-skill' and the skill authoring guide). Do NOT duplicate into src-tauri/tests/fixtures/ — a Rust regression test should reference it via the workspace path (CARGO_MANIFEST_DIR/../../skill/examples/demo-artifact.html). Duplicating would drift from the skill's canonical copy.","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:07Z","created_by":"Chris","updated_at":"2026-07-04T22:35:57Z","started_at":"2026-07-04T22:01:57Z","closed_at":"2026-07-04T22:35:57Z","close_reason":"Closed","labels":["checkpoint","m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.7","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:07Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.7","depends_on_id":"conceptify-nsy.4","type":"blocks","created_at":"2026-07-01T22:41:02Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.7","depends_on_id":"conceptify-nsy.5","type":"blocks","created_at":"2026-07-01T22:41:03Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.7","depends_on_id":"conceptify-nsy.6","type":"blocks","created_at":"2026-07-01T22:41:04Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.6","title":"CLI: save-artifact command","description":"'conceptify save-artifact --thread \u003cid\u003e --file \u003cpath\u003e' → {version:n} (PRD §5.2): uploads the file to the save-artifact endpoint, prints validation warnings to stderr (agent-visible) and the version JSON to stdout, and relies on the endpoint's side effects (live refresh + focus of the thread). This is the skill's publish step — the last command in the UC1 flow.","acceptance_criteria":"Saving from the CLI live-refreshes an open viewer and focuses the app on the thread; validation warnings appear on stderr; allowlist violations exit non-zero with the reason.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:06Z","created_by":"Chris","updated_at":"2026-07-04T21:25:25Z","started_at":"2026-07-04T21:21:14Z","closed_at":"2026-07-04T21:25:25Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.6","depends_on_id":"conceptify-36s.5","type":"blocks","created_at":"2026-07-01T22:41:02Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.6","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:06Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.6","depends_on_id":"conceptify-nsy.3","type":"blocks","created_at":"2026-07-01T22:41:01Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.5","title":"Design-system CSS scaffold + type/visual design pass (D1-D6)","description":"What makes artifacts a delight to read (PRD §6, goal G2). Build the CSS scaffold that (a) gets embedded into every artifact by the skill and (b) shares tokens with the app shell. D1 typography: pick final fonts (serif headings — Newsreader / Fraunces / Source Serif 4 candidates; Inter or system-ui body; JetBrains Mono / SF Mono code) with robust system-stack fallbacks so artifacts never break offline — this resolves OQ1. D2 tokens: type scale, spacing rhythm, ~68ch measure, full light/dark palette as CSS variables via prefers-color-scheme. D3 content components: callouts (insight/warning/definition), step sequences, key-term highlights, collapsible deep-dives (\u003cdetails\u003e), figure+caption, comparison tables. D4 code blocks: styles for pre-rendered Shiki v4 dual-theme output. D5 diagram styling hooks: same CSS variables consumed by D2/Mermaid themeVariables/hand-authored SVG (currentColor + var()); optional hand-drawn flavor. D6 motion: CSS-first, compositor-friendly (transform/opacity only — WKWebView 60fps cap), fully readable with JS disabled.","design":"PRD §6, §12 OQ1. The scaffold matters more than any diagram library choice — budget real design iteration time here.","acceptance_criteria":"A demo artifact using every D3 component + a Shiki code block + a styled SVG renders beautifully in light AND dark, in WKWebView AND Safari/Chrome, offline, with JS disabled; scaffold lives in-repo as a template the skill embeds; OQ1 font decision recorded.","notes":"OQ1 RESOLVED (D1 final typography): headings = New York via ui-serif with Iowan Old Style as the deterministic named fallback for Chromium-on-macOS (then Palatino/Georgia); body = system sans stack (SF Pro on macOS) — Inter rejected because web fonts are banned (N6, artifact-spec §1) and Inter is not preinstalled; code = SF Mono via ui-monospace with Menlo fallback — JetBrains Mono deliberately NOT listed so FR-6.3 self-review screenshots match what readers see (determinism over preference); hand-drawn flavor token --cfy-font-hand (Bradley Hand/Chalkboard SE). Visual language: 'quiet print editorial' — warm paper #fbf9f4 / warm charcoal #17140f, single terracotta accent (#a34d24 light / #e09863 dark), hairline rules, tracked small-caps labels, serif display with negative tracking. Full rationale in skill/design-system.md.\n\nDeliverables: skill/design-system.css (embeddable scaffold, tokens fenced by /* @cfy:tokens:start|end */ sentinels for later app-shell import), skill/design-system.md (OQ1 record + component vocabulary + Mermaid/d2 token mapping), skill/examples/demo-artifact.html (exemplary artifact: every D3 component, hand-written Shiki v4 vitesse-light/dark dual-theme block, two SVG diagrams — one with cfy:src d2 comment, one bespoke hand-drawn — 23 data-cfy-ids; validates against artifact-spec §8 with 0 errors 0 warnings via a file-only validator implementing the rule set). Shiki theme pair decision: vitesse-light/vitesse-dark.\n\nCRITICAL D6 FINDING (verified empirically with a headless WKWebView snapshot tool): occluded/off-screen WKWebViews SUSPEND CSS animations indefinitely — an opacity-0 fade-in with fill-mode:both stayed invisible forever in snapshots (exactly the FR-6.3 headless-review scenario). Scaffold rule adopted: no animation may hide content in its from-state; .cfy-reveal is transform-only (translateY), and there is deliberately no stroke draw-in class. The M3 skill (57l.1) must teach this rule for artifact-specific styles too.\n\nVisual verification: full-page + per-component screenshots in headless Chromium (agent-browser, light+dark, prefers-color-scheme emulated) AND genuine WKWebView (custom swift snapshot tool, light+dark via NSAppearance) — all reviewed frame by frame. JS-disabled readability is structural: the demo contains zero script tags.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:05Z","created_by":"Chris","updated_at":"2026-07-04T21:20:55Z","started_at":"2026-07-04T20:57:11Z","closed_at":"2026-07-04T21:20:55Z","close_reason":"Closed","labels":["design","m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.5","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:05Z","created_by":"Chris","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.4","title":"Artifact viewer: sandboxed iframe + version switcher + open-in-browser","description":"The reading surface (PRD §5.4, FR-2.3/2.4/2.5): thread view embeds \u003ciframe src=\"artifact://localhost/\u003cthread\u003e/\u003cversion\u003e\" sandbox=\"allow-scripts\"\u003e — NO allow-same-origin, so the artifact runs in an opaque origin and cannot touch app DOM, Tauri IPC, storage, or the API. App-shell CSP in tauri.conf.json stays strict, allowing frame-src artifact:. Subscribe to artifact-updated for live refresh. Version history switcher (FR-2.4; visual diff is P2). 'Open in browser' button opens artifact.html with the system default browser — the permanently-one-click portability guarantee (FR-2.5).","design":"PRD §5.4, §9 S2: capabilities scoped to the main window, withGlobalTauri off.","acceptance_criteria":"Artifact renders in the thread view; saving a new version refreshes the iframe in place in \u003c500ms; switcher flips between versions; open-in-browser opens the same content in the default browser; devtools confirm the iframe origin is opaque and Tauri IPC is unreachable from it.","notes":"Viewer implementation decisions (nsy.4):\n- Iframe sandbox is exactly sandbox=\"allow-scripts\" (no allow-same-origin) — S2 boundary. Verified live: contentDocument=null and contentWindow property access throws from the parent; artifact side has no IPC path.\n- The viewer loads CONCRETE version URLs (artifact://localhost/\u003cthread\u003e/\u003cN\u003e) even when the switcher is on 'Latest': appStore tracks the version list and 'latest' resolves to max known version client-side. Rationale: numeric URLs are immutable+cacheable (protocol sends immutable cache headers), and the artifact-updated payload carries the concrete version, so live refresh is a synchronous src flip (measured 11–46ms event→iframe load, well under N2's 500ms) with no round-trip on the critical path. artifact://.../latest is only a fallback while the version list is unknown.\n- handleArtifactUpdated (appStore) optimistically appends the event's version to artifactVersions, then refetches for truth; also refetches projects + threads (status flip to ready, ordering). All artifact-updated wiring lives in src/lib/events.ts -\u003e appStore seam, per the qxr.5 pattern.\n- Version switcher state (viewerVersion: number|'latest') lives in appStore, reset on thread/project change. Pinned old versions show a read-only badge and do NOT follow new saves; 'Latest' does (FR-2.4).\n- New tauri commands: list_artifact_versions (ascending; unknown thread =\u003e empty list) and open_artifact_in_browser (resolves ~/Documents/.../artifact.html server-side via new artifacts::latest_copy_path helper, opens via tauri_plugin_opener::open_path; returns the path). Frontend never builds filesystem paths (FR-2.5).\n- App-shell CSP set in tauri.conf.json: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self' data:; connect-src ipc: http://ipc.localhost; frame-src artifact:; object-src 'none'; base-uri 'none'; form-action 'none'. devCsp additionally allows ws://localhost:1420 http://localhost:1420 for Vite HMR.\n- CSP enforcement facts (verified empirically on the built app, tauri build --debug): Tauri v2 delivers the CSP via response header on the tauri:// protocol (no meta tag in DOM); eval() is blocked (script-src enforced, EvalError) and an external img fires securitypolicyviolation (img-src enforced) while Tauri IPC keeps working. In DEV (remote devUrl -\u003e Vite), Tauri cannot apply a CSP to the page at all — devCsp is effectively inert with a devUrl; the production CSP is the load-bearing one. Do not assume dev mode tests the CSP.\n- Generating/updating with no versions =\u003e pulsing 'Generating artifact…' placeholder (keeps the question card); status error with no versions =\u003e failure state (logs are M5); unknown/edge versions inside the iframe are handled by the protocol's styled error pages.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:04Z","created_by":"Chris","updated_at":"2026-07-04T21:52:14Z","started_at":"2026-07-04T21:33:11Z","closed_at":"2026-07-04T21:52:14Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.4","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:04Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.4","depends_on_id":"conceptify-nsy.2","type":"blocks","created_at":"2026-07-01T22:40:59Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.4","depends_on_id":"conceptify-nsy.3","type":"blocks","created_at":"2026-07-01T22:41:00Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.4","depends_on_id":"conceptify-qxr.4","type":"blocks","created_at":"2026-07-01T22:41:00Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.3","title":"save-artifact endpoint: validation, versioning, storage layout, live refresh","description":"The ingestion path (FR-3.6, §5.6): POST save-artifact stores an HTML file for a thread as a new version (v1, v2, ...; prior versions retained; created_by initial|follow_up), using the §5.6 layout — threads/\u003cslug\u003e/artifact.vN.html + artifact.html copy/symlink of latest + runs/ dir (final root per decision OQ2). Writes are atomic (temp + rename, N4). Validation per the artifact spec: parseable HTML, size sanity warn \u003e ~5MB, no \u003cscript src\u003e outside the Tier-2 allowlist (hard fail), data-cfy-id presence (warn when coverage thin); warnings surface to the caller as structured JSON, not hard failures. On success: thread status → ready, artifact-updated Tauri event → viewer live-refresh (\u003c 500ms, N2).","design":"PRD §7.3 FR-3.6, §5.6, N2/N4. Blocked on decision OQ2 for the storage root.","acceptance_criteria":"Saving twice yields v1 and v2 with both files on disk and latest pointer updated; a disallowed \u003cscript src\u003e is rejected; thin data-cfy-id coverage and \u003e5MB produce warnings in the JSON response; kill -9 during save never leaves a corrupt/partial visible artifact; artifact-updated event fires with thread + version.","notes":"Implemented. Key decisions:\n\n1. REQUEST SHAPE: raw HTML bytes (POST /api/v1/threads/{id}/artifact, Content-Type: text/html, not enforced) — not JSON/multipart. A JSON wrapper cannot carry invalid UTF-8, making the spec's E-UTF8 rule unreachable, and escapes multi-MB payloads; raw bytes let the validator own E-UTF8/E-SIZE-MAX verbatim and the CLI just streams the file. DefaultBodyLimit=60MiB on this route so 50–60MiB files get the structured E-SIZE-MAX instead of a bare 413.\n\n2. created_by INFERRED: version 1 = initial, \u003e=2 = follow_up. Never caller-supplied.\n\n3. VALIDATOR: scraper (html5ever) with manual tree traversal (no CSS selectors — avoids SVG namespace pitfalls for shape counting / data-cfy-id checks). All spec §8 rules implemented verbatim incl. the §7.1 allowlist match rule (prefix + next-char ./ + charset + no .. segment, case-sensitive), @import scanning in inline CSS (code position → E-EXTERNAL-CODE; relative @import → W-LOCAL-REF), cfy:src comment adjacency (W-SRC-ORPHAN), W-VERSION-MISMATCH against the server-assigned version. Hand-rolled id-grammar/kv/css scanners — no regex crate.\n\n4. ATOMICITY/ORDERING (N4): whole pipeline (version query → validate → temp+rename file writes → single DB tx inserting artifact row + thread→ready) runs inside the one shared connection lock (db::with_conn_result closure), so concurrent saves serialize — no version races. Files written BEFORE the DB row: a crash between leaves an orphan file no row references (invisible; overwritten on retry since version derives from the DB), never a row pointing at a missing file. artifact.html is an atomic COPY of the latest version, temp+rename, never a symlink.\n\n5. ApiState MADE GENERIC over tauri::Runtime (default Wry — zero production change) so HTTP-level tests drive the REAL router (auth middleware + routing + emit) against tauri::test MockRuntime. Future route beads (comments, get-context) inherit this harness (see server/artifacts_routes.rs tests::Harness). artifacts_root() has a cfg(test)-only CONCEPTIFY_TEST_ARTIFACTS_DIR env override (process-wide → set once via OnceLock in tests).\n\n6. Error response: 422 with { error, code } (spec shape, first violation) plus errors[] listing all hard failures. Event artifact-updated payload: { project_id, thread_id, version } (docs/api.md Events table).\n\n7. No migration needed — the M0 artifacts table already had version/file_path/created_by/UNIQUE(thread_id,version).\n\nFor nsy.6 (CLI): read file as bytes, POST raw body, print warnings[] as 'warning: CODE: message' to stderr, exit non-zero on non-2xx using code/error fields. For nsy.4 (viewer): listen artifact-updated, reload iframe for thread_id/version. For nsy.2: files live at ~/Documents/conceptify/artifacts/\u003cproject-id\u003e/threads/\u003cslug\u003e/artifact.vN.html; artifacts.file_path DB column holds the absolute vN path.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:03Z","created_by":"Chris","updated_at":"2026-07-04T21:20:54Z","started_at":"2026-07-04T20:57:11Z","closed_at":"2026-07-04T21:20:54Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.3","depends_on_id":"conceptify-36s.7","type":"blocks","created_at":"2026-07-01T22:40:58Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.3","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:03Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.3","depends_on_id":"conceptify-nsy.1","type":"blocks","created_at":"2026-07-01T22:40:57Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.3","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:40:58Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.2","title":"artifact:// protocol handler + per-response CSP","description":"Serve artifacts through a custom URI scheme (PRD §5.4): register_asynchronous_uri_scheme_protocol for artifact://localhost/\u003cthread\u003e/\u003cversion\u003e — handler reads the HTML from disk, injects the bridge script (injection point built now; the bridge itself is M4 — inject a no-op stub until then), and responds with a restrictive per-response CSP: allow inline script/style, connect-src 'none' (artifact JS can never reach the localhost API), allow only the §8 Tier-2 pinned CDN hosts. Server-side injection keeps the .html file on disk pristine and portable — no Conceptify residue (G3).","design":"PRD §5.4, §9 S2, Appendix A. Cross-scheme = real origin isolation. Note wry #168: inline SVG works, \u003cimg src\u003e via custom protocol has gotchas.","acceptance_criteria":"artifact:// URL renders a test HTML file; response carries the CSP header; a fetch() from artifact JS to 127.0.0.1:4477 is blocked; the on-disk file is byte-identical after serving; unknown thread/version returns a clean error page.","notes":"IMPLEMENTED (src-tauri/src/artifact_protocol.rs; wiring in lib.rs; version_file_path helper appended to artifacts.rs; docs/api.md gained 'The artifact:// scheme' section — the URL contract nsy.4 consumes).\n\nURL CONTRACT: artifact://localhost/\u003cthread-id\u003e/\u003cversion\u003e. \u003cversion\u003e = bare decimal integer \u003e= 1 (serves immutable artifact.vN.html, Cache-Control: public,max-age=31536000,immutable) OR literal lowercase 'latest' (resolved via DB MAX(version), serves that versioned file, no-store). DECISION: latest is DB-resolved, NOT the artifact.html disk copy — files are written before the DB row commits (N4 crash ordering), so the copy can be ahead of the DB; DB is the source of truth the version switcher/events read. GET only (405 otherwise). No percent-decoding anywhere: segments validated against [A-Za-z0-9_-]{1,128} / digits, so %, ., backslash, empty segments all 400 — traversal structurally impossible; DB-returned project_id/slug re-validated as single path segments (defense in depth vs corrupted DB).\n\nCSP (every response, success + error pages, verbatim spec section-3 reference policy): default-src 'none'; script-src 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'unsafe-inline' https://cdn.jsdelivr.net; font-src data: https://cdn.jsdelivr.net; img-src data:; connect-src 'none'. Plus text/html;charset=utf-8 and nosniff. Keep in lockstep with spec 7.1 allowlist + artifacts::CDN_ALLOWLIST.\n\nBRIDGE INJECTION (for 94m.1): single seam = artifact_protocol::inject_bridge + BRIDGE_STUB const. Stub is \u003cscript data-cfy-bridge=\"stub\"\u003e defining window.__cfyBridge = Object.freeze({stub:true}); spliced before the LAST case-insensitive \u003c/body\u003e (last occurrence skips \u003c/body\u003e literals in script text; whole DOM parsed when it runs — no DOMContentLoaded needed), appended at EOF if no close tag, idempotent via the reserved data-cfy-bridge marker. M4 replaces BRIDGE_STUB content only; injection point/mechanism are final. Byte-splice, never re-encodes; disk file never touched (G3).\n\nFOR nsy.4: iframe sandbox=\"allow-scripts\" (no allow-same-origin); app-shell CSP in tauri.conf.json is still null — when you set it, allow frame-src artifact:. Use concrete version from artifact-updated payload (or /latest). Never point subresources at artifact:// (wry#168).\n\nVERIFICATION: 16 unit tests on the factored core (parse incl. traversal/encoding, DB resolution incl. latest fallback, exact CSP string, stub placement/idempotence, pristine-disk check, styled 404s/400/405/500, HTML-escaping of reflected path input); tests save through the real artifacts::save_artifact so file naming is pinned in lockstep. cargo test --workspace 81 pass; just check clean. Live: app launched with isolated HOME — scheme registration doesn't disturb startup. NOT live-exercised: an actual WKWebView load of artifact:// and the fetch()-blocked-by-CSP behavior (needs the nsy.4 iframe to drive; CSP header value is exact per spec, enforcement is WebKit's).","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:02Z","created_by":"Chris","updated_at":"2026-07-04T21:32:59Z","started_at":"2026-07-04T21:21:14Z","closed_at":"2026-07-04T21:32:59Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.2","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:01Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.2","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:40:56Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.1","title":"Author the artifact specification doc (FR-3.x contract)","description":"Write the formal contract between generating agents and the app (PRD §7.3) as an in-repo doc — it is later published inside the skill verbatim. Must specify: single self-contained .html (inline CSS/SVG/JS, no local file refs, network only from the §8 Tier-2 pinned-CDN allowlist with graceful degradation) (FR-3.1); WKWebView + browser dual-rendering constraint, Safari-compatible only (FR-3.2); anchorability — stable data-cfy-id on every section heading and meaningful diagram element (FR-3.3); diagram DSL sources embedded as HTML comments adjacent to rendered SVG (FR-3.4); metadata block cfy:question / cfy:version / cfy:generated-by in \u003chead\u003e (FR-3.5); and the validation rules save-artifact enforces (FR-3.6).","design":"PRD §7.3, §8. This doc is the single source of truth — validator and skill must both cite it rather than restate it.","acceptance_criteria":"Doc exists in-repo covering FR-3.1 through FR-3.6 precisely enough that (a) the save-artifact validator can be implemented from it and (b) the skill can embed it as authoring rules; Tier-2 allowlist enumerated with pinned versions.","notes":"Spec written at docs/artifact-spec.md. Decisions taken where PRD left latitude:\n- data-cfy-id grammar: dot-separated kebab-case segments, regex ^[a-z](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z](?:[a-z0-9-]*[a-z0-9])?)*$, max 64 chars; semantic (never positional) naming mandated; diagram elements namespaced \u003cfigure-id\u003e.\u003celement\u003e; deterministic derivation from DSL names (lowercase, -\u003e becomes -to-, non-alnum runs collapse to -) so regeneration reproduces ids.\n- Diagram source comment: \u003c!--cfy:src lang=.. for=.. renderer=..--\u003e, body escaped (\\ then --\u003e -\u003e --\\\u003e , --!\u003e -\u003e --!\\\u003e) because Mermaid arrows (--\u003e) would terminate an HTML comment; decode is single pass \\\\-\u003e\\ , \\\u003e-\u003e\u003e. Tier-2 runtime-rendered figures satisfy FR-3.4 via their mandatory \u003cpre\u003e fallback, no duplicate comment.\n- Allowlist: single host cdn.jsdelivr.net (npm mirror) so CSP has one CDN host; prefix match with next-char-in {./} rule allows both @11 and @11.15.0 pins; charset [A-Za-z0-9@._/-] + no .. excludes query/fragment/escapes. highlight.js via @highlightjs/cdn-assets@11; markmap = markmap-lib/-view/-toolbar@0; ELK via @mermaid-js/layout-elk@0. Web fonts NOT allowlisted yet (deferred to M2 design pass / OQ1).\n- Validation: rule IDs E-*/W-*. Hard: E-UTF8, E-PARSE (empty body), E-EXTERNAL-CODE (script src/link stylesheet|preload|modulepreload/@import outside allowlist), E-SIZE-MAX 50 MiB (added backstop beyond PRD letter). Warn: W-SIZE 5 MiB; thin coverage = any h1-h4 without id, or svg with \u003e=6 shape elems and \u003c3 anchored elems; plus doctype/charset/title/meta/version-mismatch/id-format/id-dup/src-malformed/src-orphan/external-ref/local-ref.\n- cfy:version is informational; server-assigned version authoritative, mismatch = W-VERSION-MISMATCH.\n- Reference runtime CSP documented in spec §3 (default-src none; script/style unsafe-inline + cdn.jsdelivr.net; font data:+cdn; img data:; connect none) — protocol handler must be at least this restrictive.","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:01Z","created_by":"Chris","updated_at":"2026-07-04T20:56:02Z","started_at":"2026-07-04T20:46:27Z","closed_at":"2026-07-04T20:56:02Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.1","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-01T22:40:00Z","created_by":"Chris","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr.5","title":"Tauri event wiring: live list updates","description":"Close the live-update loop (PRD §5.3): the frontend subscribes to Tauri events emitted by the Rust core (thread-created, project changes; later artifact-updated, comment-resolved) and updates project/thread lists in place — no polling. This is the pattern all later live features (viewer refresh, sidebar answers) reuse, so establish clean event names and a single subscription layer.","acceptance_criteria":"With the app visible, running 'conceptify create-thread' from a terminal makes the new thread appear in the list without any user interaction; event subscription is centralized (one module), not scattered per-component.","notes":"Implemented in src/lib/events.ts (single subscription module) + wired once from App.tsx useEffect (returns cleanup). Event-\u003estore mapping: projects-changed -\u003e refetchProjects; thread-created -\u003e refetchProjects + refetchThreads(payload.project_id); navigate -\u003e refetchProjects, selectProject, then (if thread_id) AWAIT refetchThreads(project_id) BEFORE selectThread so a just-created target thread is in the list and the selection sticks (refetchThreads is a no-op unless its project is the selected one, which selectProject just made it). docs/api.md Events table verified accurate vs the Rust emit sites (payload field names/nullability match) - left untouched.\n\nVERIFICATION GOTCHAS for downstream live-event beads (viewer-refresh, sidebar-answers):\n- Tauri emit-\u003eJS listen was verified end-to-end via a temporary DB-observable proof (handler invoked rename_project with a sentinel derived from the event's thread_id; confirmed the isolated SQLite DB name changed to CFY_REACTED_\u003cthread8\u003e). Removed after.\n- The proper harness is 'npm run tauri dev'. A directly-launched raw binary (./target/debug/conceptify-app with isolated HOME) started the server and webview but did NOT deliver emit-\u003eJS-listen events in that headless launch - use tauri dev to verify live events.\n- Isolating HOME for app state breaks rustup ('no default toolchain') because it reads $HOME/.rustup; set RUSTUP_HOME=/Users/chrismeehan/.rustup and CARGO_HOME=/Users/chrismeehan/.cargo (real home) while keeping HOME=\u003ctmp\u003e so cargo works but the app writes state under the tmp HOME.\n- Tauri v2 does NOT forward webview console.log to the dev terminal, and WKWebView console is not in os_log; a DB-observable temp invoke is the reliable external proof.","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:00Z","created_by":"Chris","updated_at":"2026-07-04T21:03:03Z","started_at":"2026-07-04T20:45:19Z","closed_at":"2026-07-04T21:03:03Z","close_reason":"Closed","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr.5","depends_on_id":"conceptify-qxr","type":"parent-child","created_at":"2026-07-01T22:39:59Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.5","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:40:56Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.5","depends_on_id":"conceptify-qxr.4","type":"blocks","created_at":"2026-07-01T22:40:55Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr.4","title":"App shell UI: project sidebar + thread list + navigation","description":"The navigation shell (PRD §5.3, FR-1.3, FR-2.2): project list sidebar (thread counts, last-activity, rename, archive; badge + re-map affordance for projects whose mapped directory no longer exists) → thread list per project (sorted by last activity, status chips generating/ready/updating/error, open-comment counts) → an empty thread-view placeholder that M2 fills with the artifact viewer. Keyboard-first navigation where cheap (⌘K switcher is Phase 2).","acceptance_criteria":"Can navigate project → thread list → thread; rename and archive work from the UI; a project whose directory was deleted shows a badge and offers re-mapping; status chips render all four states.","notes":"Shell UI implemented (frontend in src/**, plus a small new src-tauri/src/commands.rs + additive registration in lib.rs). Do NOT close per orchestrator.\n\nDATA-ACCESS DECISION (important for downstream): the frontend reaches the core via #[tauri::command] invoke, NOT the HTTP API. Reason: the webview origin (tauri://localhost) is cross-origin to 127.0.0.1:\u003cport\u003e, so every bearer-auth'd route triggers a CORS preflight the axum server doesn't answer (no CORS layer, which lives in the off-limits server/**); also only Rust can read the token/port files and stat the filesystem for the FR-1.3 'missing directory' badge. Commands added in src-tauri/src/commands.rs: list_projects, list_threads, rename_project, set_project_archived, remap_project. They wrap the existing projects/threads domain fns; rename_all=snake_case so JS invoke keys match Rust params. list_projects DTO adds root_exists (command-only enrichment; the HTTP ProjectListItem lacks it).\n\nRE-MAP: no HTTP/domain endpoint existed for updating root_path. Added remap_project as a command with the canonicalize+UPDATE inlined in commands.rs (NOT added to projects.rs — the parallel agent was actively editing projects.rs, so I avoided it to prevent conflicts). UI uses a text-path input; when bead 959.3 lands the dialog plugin, swap it for a native dir picker.\n\nFOR qxr.5 (event wiring): central store is src/store/appStore.ts (module-level observable + useAppStore hook). Seams: appStore.refetchProjects() and appStore.refetchThreads(projectId). Wire listen('projects-changed')-\u003erefetchProjects and listen('thread-created', e)-\u003e{refetchProjects(); refetchThreads(e.payload.project_id)} in a top-level effect (example in appStore.ts header comment). My command mutations do NOT emit Tauri events; the store refetches after its own mutations, so qxr.5 only needs to cover CLI/skill-originated events. refetchThreads(id) is a no-op unless id is the selected project, and both refetches guard against stale/out-of-order results.\n\nGates: npm run build, just check (clippy -D warnings), cargo test --workspace all pass. Added an IPC-level test (commands::tests) driving invoke('list_projects',{include_archived}) through Tauri dispatch to prove arg-mapping + root_exists. GUI not driven headlessly (WKWebView).","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:59Z","created_by":"Chris","updated_at":"2026-07-04T20:45:10Z","started_at":"2026-07-04T20:27:38Z","closed_at":"2026-07-04T20:45:10Z","close_reason":"Closed","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr.4","depends_on_id":"conceptify-36s.1","type":"blocks","created_at":"2026-07-01T22:40:53Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.4","depends_on_id":"conceptify-qxr","type":"parent-child","created_at":"2026-07-01T22:39:58Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.4","depends_on_id":"conceptify-qxr.1","type":"blocks","created_at":"2026-07-01T22:40:54Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.4","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:40:54Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr.3","title":"CLI: ensure-project, create-thread, open","description":"Grow the CLI command surface (PRD §5.2) over the M1 endpoints: 'conceptify ensure-project --dir \u003cpath\u003e [--name \u003cname\u003e]' → {projectId, created}; 'conceptify create-thread --project \u003cid\u003e --title \u003ct\u003e --question \u003cq\u003e' → {threadId}; 'conceptify open --thread \u003cid\u003e | --project \u003cid\u003e' → focuses the app on the target (bring window to front, navigate). JSON on stdout — these are the exact commands the M3 skill will drive.","design":"PRD §5.2 command table. Focus-on-open is part of UC1's feel: the artifact should be on screen when the agent finishes.","acceptance_criteria":"Each command works against a running app and via the cold-launch path; output is stable parseable JSON; open brings the app window to the foreground showing the right project/thread.","notes":"Implemented ensure-project, create-thread, open (qxr.3).\n\nNew endpoint: POST /api/v1/open (authed). Body: exactly one of {thread_id} or {project_id}; thread_id wins if both. Validates target -\u003e 404 (thread/project not found), 400 if neither. On success: brings 'main' window to front (show()+set_focus() — window HIDES on close so must show first) and emits Tauri event 'navigate' with {project_id, thread_id|null}. Response {ok, project_id, thread_id}. Documented in docs/api.md.\n\nFRONTEND (qxr.4/qxr.5): subscribe to 'navigate' event {project_id, thread_id: string|null} to route to the target. thread_id null =\u003e open project view, no specific thread.\n\nCLI output contract is camelCase (projectId, threadId) even though the HTTP API is snake_case — the CLI is the stable contract the M3 skill parses. create-thread output includes 'slug' (needed for save-artifact artifact folder). ensure-project output is exactly {projectId, created} per PRD table.\n\nKey decision: CLI canonicalizes --dir to an absolute path BEFORE sending, because the server canonicalizes relative to ITS cwd (wherever the app launched), not the agent's cwd — a bare relative --dir would resolve wrong. Nonexistent --dir fails fast on the CLI.\n\nKept deps lean: hand-rolled --key value flag parser (no clap), matching the existing skeleton's style. authed_post() helper reads token file, sends Bearer, surfaces server {error} messages verbatim on stderr with exit 1.\n\nVerified end-to-end against a live debug app binary (isolated HOME so the real user DB was untouched): all 3 commands return correct JSON; idempotent ensure-project (created:true then false, same id); slug dedupe (-2); open --thread resolves to its project; open --project =\u003e threadId null; server logs showed no 'no main window'/emit-failure lines (focus+navigate emit ran clean). Raw curl confirmed /open: 401 no-auth, 400 no-target, 404 unknown, 200 valid. Cold-launch path (open -a Conceptify) unchanged skeleton code — not re-driven since it would launch the stale release bundle, not this new code. 27 tests pass; clippy -D warnings clean.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:58Z","created_by":"Chris","updated_at":"2026-07-04T20:40:18Z","started_at":"2026-07-04T20:27:38Z","closed_at":"2026-07-04T20:40:18Z","close_reason":"Closed","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr.3","depends_on_id":"conceptify-36s.5","type":"blocks","created_at":"2026-07-01T22:40:51Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.3","depends_on_id":"conceptify-qxr","type":"parent-child","created_at":"2026-07-01T22:39:57Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.3","depends_on_id":"conceptify-qxr.1","type":"blocks","created_at":"2026-07-01T22:40:52Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.3","depends_on_id":"conceptify-qxr.2","type":"blocks","created_at":"2026-07-01T22:40:52Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr.2","title":"Threads backend: create + list (API)","description":"Thread lifecycle basics (FR-2.1, FR-2.2 backend): create a thread (project_id, title, initial_question; slug generated from title for the artifact folder; status starts 'generating' per OQ4's current spec — create early, error status + retry on failure) and list threads per project sorted by last activity with status + open-comment counts. Mutations emit Tauri events (thread-created etc.).","design":"PRD §7.2, §4 Thread entity. Status machine: generating | ready | updating | error — transitions owned by save-artifact and run lifecycle in later milestones.","acceptance_criteria":"Thread create returns id + slug; slugs are filesystem-safe and unique within a project; list is sorted by last activity and includes status and open-comment count; thread-created event fires.","notes":"IMPLEMENTATION (qxr.2 threads backend create+list):\n\n- SLUG PERSISTED AS A COLUMN, not derived on the fly. Added via APPENDED migration #7 (THREAD_SLUG) in db/migrations.rs, NOT by editing the shipped THREADS constant (append-only rule in that file). Verified: real dev DB was at user_version=6 with no slug column; editing THREADS in place would have broken it. Migration = 'ALTER TABLE threads ADD COLUMN slug TEXT NOT NULL DEFAULT ''; CREATE UNIQUE INDEX idx_threads_project_slug ON threads(project_id, slug)'. SQLite can't add a UNIQUE column inline, hence the separate composite unique index. DEFAULT '' only affects pre-existing rows (there are none). Verified the 6-\u003e7 upgrade path non-destructively against a copy of the real dev DB.\n\n- Slug is the STABLE artifact-folder name (§5.6 \u003cthread-slug\u003e), so it MUST stay put if the thread title is later renamed — downstream rename/save-artifact beads must NOT regenerate the slug from the (new) title. Persisted precisely so it's stable.\n\n- slugify(): ASCII-only, dependency-free (no transliteration crate). Lowercases alphanumerics, collapses every other char (whitespace/punct/path-seps/non-ASCII) to single hyphens, no leading/trailing hyphen, capped at 80 chars. Titles that reduce to empty (emoji/CJK/punctuation-only) fall back to 'thread'. Per-project uniqueness via dedupe_slug (slug, slug-2, slug-3...), mirroring projects::dedupe_name.\n\n- open_comment_count is a REAL LEFT JOIN on the existing comments table (COUNT WHERE status='open'), not a literal 0 — stays correct once comments backend (94m.2) inserts rows.\n\n- ThreadStatus enum (generating|ready|updating|error) defined in threads.rs. Only Generating produced on create; other variants constructed by from_db_str() on read (keeps all 4 live under clippy -D warnings). Transitions past 'generating' owned by later beads.\n\n- EVENT: POST /threads emits 'thread-created' with payload { project_id, thread_id } (NOT null like projects-changed) so qxr.5 can refresh just the affected project's list. Documented in docs/api.md.\n\n- ROUTES (flat, mirroring projects): POST /api/v1/threads (project_id/title/initial_question in body), GET /api/v1/threads?project_id=\u003cid\u003e. Unknown project_id -\u003e list returns empty array (no 404), matching list_projects; create -\u003e 404 ProjectNotFound, 400 EmptyTitle.\n\nFILES: crates/conceptify-types/src/lib.rs (CreateThread{Request,Response}, ThreadListItem, ListThreadsResponse); src-tauri/src/threads.rs (new domain module + unit tests); src-tauri/src/db/migrations.rs (THREAD_SLUG); src-tauri/src/server/threads_routes.rs (new); server/mod.rs + routes.rs (wiring); lib.rs (mod threads + real-migration integration test); docs/api.md.\n\nNOT closed/committed per orchestrator instruction. just check + cargo test (13 passed) green.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:57Z","created_by":"Chris","updated_at":"2026-07-04T20:26:49Z","started_at":"2026-07-04T20:16:32Z","closed_at":"2026-07-04T20:26:49Z","close_reason":"Closed","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr.2","depends_on_id":"conceptify-qxr","type":"parent-child","created_at":"2026-07-01T22:39:56Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.2","depends_on_id":"conceptify-qxr.1","type":"blocks","created_at":"2026-07-01T22:40:50Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":6,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr.1","title":"Projects backend: ensure-project, create, list, rename, archive (API)","description":"Project CRUD in the Rust core (FR-1.1, FR-1.3 backend): ensure-project by directory — given a root path, canonicalize it and return the existing project or create one, defaulting name to the directory name (deduped with a numeric suffix if taken); one project per path (unique root_path). Plus list (with thread counts + last-activity), rename, and archive (hide, don't delete). Expose under /api/v1/. Mutations emit Tauri events for live UI (§7.8).","design":"PRD §7.1, §4 Project entity. Canonicalization is the identity rule — symlinks/trailing slashes must not create duplicate projects.","acceptance_criteria":"POST ensure-project twice with the same dir (including a symlinked variant of the path) returns the same project with created:false the second time; name collisions dedupe with suffix; archive hides from default list but doesn't delete; list returns thread counts and last-activity; mutations emit events.","status":"closed","priority":1,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:56Z","created_by":"Chris","updated_at":"2026-07-04T05:45:47Z","started_at":"2026-07-04T05:34:35Z","closed_at":"2026-07-04T05:45:47Z","close_reason":"Projects API complete: ensure (canonicalized identity + name dedup), list (thread_count/last_activity, archived filter), rename, archive/unarchive; projects-changed events; all criteria verified live; types shared via conceptify-types","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr.1","depends_on_id":"conceptify-36s.2","type":"blocks","created_at":"2026-07-01T22:40:49Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.1","depends_on_id":"conceptify-36s.3","type":"blocks","created_at":"2026-07-01T22:40:50Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-qxr.1","depends_on_id":"conceptify-qxr","type":"parent-child","created_at":"2026-07-01T22:39:55Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.7","title":"OQ2: artifact storage location — in-repo .conceptify/ vs centralized","description":"Open question 2 (PRD §12): where do artifact files live? Option A (current §5.6 assumption): \u003cproject_root\u003e/.conceptify/threads/\u003cslug\u003e/artifact.vN.html — travels with the repo, human-visible, but pollutes it (needs global-gitignore suggestion; skill must never commit it). Option B: centralized ~/Documents/conceptify/artifacts/\u003cproject\u003e/ — clean repos, less portable. PRD calls this 'cheap to flip in M0 — decide before M2' because the save-artifact storage layout (M2) bakes it in. Decide and record the rationale here; update §5.6 if flipping.","acceptance_criteria":"A decision is recorded (with rationale) and the save-artifact storage task is unblocked with a concrete path scheme; prd.md updated if the §5.6 assumption changes.","notes":"DECISION: Option B — centralized artifact storage, keyed by project id, NOT in-repo.\n\nRATIONALE:\n1. The stated benefit of Option A (\"travels with the repo\") is negated by its own constraint: .conceptify/ must never be committed, so it can never actually travel via the mechanism repos travel by (clone/push/pull). It would only travel via non-git copies (rsync/tar), which is not how this codebase's users move repos around.\n2. Concrete data-loss risk unique to in-repo storage: any routine git-hygiene command a developer runs inside the repo — `git clean -fdx`, `git clean -fd`, careless `rm -rf` on a \"throwaway\" clone — silently deletes an untracked .conceptify/ directory with zero recourse, while the SQLite metadata DB (in Application Support, untouched) still references now-missing files. That's a split-brain state directly contrary to N4's \"never corrupt state\" spirit, and the app has no way to intercept it since it's a plain filesystem/git operation outside Conceptify's control.\n3. Repo directories are more volatile than the artifacts they'd host: users delete/re-clone/rename/relocate working directories far more casually than they'd want to lose saved Q\u0026A + comment history. FR-1.3 already anticipates root_path going missing (badge + re-map affordance) — that volatility is a known hazard, not a hypothetical. Centralizing decouples artifact lifetime from root_path entirely: moving/renaming/deleting the mapped directory only requires repairing the root_path mapping, never touches artifact files.\n4. Removes real engineering/UX surface: no \"suggest global gitignore\" onboarding step, no defensive \"skill must never commit this\" hazard, no risk of an errant `git add -A`/`git add .` scooping multi-MB HTML artifacts into a commit/PR history permanently.\n5. Simpler for M2 (conceptify-nsy.3): one storage root, one code path, independent of whether a project is a mapped repo or an auto-created topic folder (auto-created projects already lived under ~/Documents/conceptify/projects/\u003cname\u003e/ as their root_path — this decision just stops conflating \"the project's working directory\" with \"where Conceptify's own artifact files live\").\n6. Cost accepted: less casually human-visible in Finder next to the code, and nominally \"less portable\" if someone expected to hand off a repo folder with artifacts baked in — both mitigated by existing/planned features: FR-2.5 \"Open in browser\" is the per-artifact portability guarantee, and Phase-2 already plans export/share bundling for deliberate hand-off.\n\nPATH SCHEME (concrete, replaces §5.6 in-repo layout):\n\n~/Documents/conceptify/\n  artifacts/\n    \u003cproject-id\u003e/                 (opaque DB primary key; immutable across rename/re-map, FR-1.3)\n      threads/\n        \u003cthread-slug\u003e/\n          artifact.v1.html\n          artifact.v2.html        \u003c- follow-up updates append versions, monotonically increasing per thread\n          artifact.html           \u003c- atomic COPY (not symlink) of the latest version; written via temp+rename same as versioned files (N4), so it can never dangle/go stale\n          runs/\u003crun-id\u003e.log       \u003c- headless agent transcripts\n  projects/\n    \u003cslugified-name\u003e/             (unchanged — root_path for auto-created/non-codebase projects only; a working directory for agents, orthogonal to artifact storage above)\n\nThread-slug is generated once at thread creation (FR-2.1) and never renamed, so its directory never churns. Project-id (not project name) is the storage key so renaming a project (FR-1.3) never requires moving files on disk. save-artifact's \"validates, stores\" semantics (§5.2) mean the agent can author its draft HTML anywhere (its own cwd is fine) and hand a --file path to the CLI; the Rust core copies/moves it into this canonical location — so nothing about the skill's authoring flow (FR-6.1) depended on in-repo storage in the first place.\n\nPRD EDITS: §5.6 storage layout block and prose rewritten to the centralized scheme above (removed the global-gitignore footnote, since it's no longer needed); §12 OQ2 marked RESOLVED with a one-paragraph pointer back to this bead.\n\nUNBLOCKS: conceptify-nsy.3 (save-artifact endpoint) can now implement against this concrete layout.","status":"closed","priority":1,"issue_type":"decision","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:55Z","created_by":"Chris","updated_at":"2026-07-02T06:01:36Z","started_at":"2026-07-02T05:57:13Z","closed_at":"2026-07-02T06:01:36Z","close_reason":"Decided Option B: centralized ~/Documents/conceptify/artifacts/\u003cproject-id\u003e/ storage; rationale in notes and prd.md §12","labels":["m0","open-question","phase1"],"dependencies":[{"issue_id":"conceptify-36s.7","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:54Z","created_by":"Chris","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.5","title":"conceptify CLI skeleton: shared workspace + launch-and-wait contract","description":"CLI foundation (PRD §5.2): a small Rust binary in the same Cargo workspace as the app (shared request/response types with the server — no Node dependency, instant start). Implement the liveness/launch contract every command uses: probe GET /health (reading the port file, falling back to 4477), on failure run 'open -a Conceptify', poll /health up to ~10s, then proceed. Read the bearer token from the token file. Implement 'conceptify status' (app/API health, version) as the first real command; the command surface grows in later milestones. JSON output on stdout for agent consumption, human-readable errors on stderr.","design":"PRD §5.2. This contract deliberately sidesteps macOS single-instance argv-forwarding quirks (plugins-workspace #1613) — 'launch and focus' is just HTTP.","acceptance_criteria":"With the app stopped, 'conceptify status' launches the app, waits for /health, and prints JSON health info; with the app running, it responds in \u003c50ms (N3); exits non-zero with a clear message if the app can't be reached within the timeout.","notes":"Orchestrator addendum: the app crate and CLI both produced a binary named 'conceptify' in the shared workspace target/ (cargo output filename collision, last-built-wins). Fixed by renaming the app binary to 'conceptify-app' ([[bin]] in src-tauri/Cargo.toml + mainBinaryName in tauri.conf.json); the bundler still ships Conceptify.app via productName. CLI owns the 'conceptify' name. E2E verified: with app running, 'conceptify status' returns correct JSON in \u003c10ms warm (first-ever run ~1s due to macOS Gatekeeper verification of a freshly built binary, not CLI logic).","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:52Z","created_by":"Chris","updated_at":"2026-07-04T05:26:47Z","started_at":"2026-07-04T05:05:11Z","closed_at":"2026-07-04T05:26:47Z","close_reason":"CLI skeleton complete: workspace (src-tauri + conceptify-types + conceptify-cli), launch-and-wait contract, 'conceptify status' verified E2E \u003c10ms; app binary renamed conceptify-app to resolve target/ collision","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.5","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:52Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.5","depends_on_id":"conceptify-36s.2","type":"blocks","created_at":"2026-07-01T22:40:47Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.3","title":"SQLite layer: rusqlite + migrations + §4 schema","description":"Database foundation (PRD §5.1, §5.6): rusqlite (bundled SQLite, WAL mode) held in Tauri managed state so both #[tauri::command] handlers and axum handlers share it. Explicitly NOT tauri-plugin-sql (no first-class Rust-side query API — tauri discussion #11527). Migrations via rusqlite_migration or a user_version scheme. Implement the §4 domain model schema: projects (id, name, root_path UNIQUE, created_at, archived), threads (id, project_id, title, initial_question, status generating|ready|updating|error, created_at, updated_at), artifacts (id, thread_id, version, file_path, created_at, created_by initial|follow_up), comments (id, thread_id, artifact_version, anchor JSON, body, status open|answered|applied, answer_html, created_at, resolved_at), follow_up_runs (id, thread_id, agent, model, mode answer|apply, status, log_path, started_at, finished_at), settings. DB stores artifact paths, never HTML blobs.","design":"PRD §4, §5.6 rationale: metadata needs transactional integrity → SQLite; artifacts stay as plain files on disk.","acceptance_criteria":"DB created at ~/Library/Application Support/conceptify/conceptify.db in WAL mode; migrations run idempotently on startup; all six §4 entities have tables with the specified fields and constraints (unique root_path); both a tauri command and an axum handler can query through managed state.","notes":"Implementation notes (bd conceptify-36s.3):\n\nModule layout: src-tauri/src/db/{mod,migrations}.rs. `db::DbHandle` =\nArc\u003cMutex\u003crusqlite::Connection\u003e\u003e, opened once in db::init() (called from\nlib.rs's setup hook, before server::start is spawned) and put into\napp.manage(db). server::start() pulls it back out via\napp_handle.state::\u003cdb::DbHandle\u003e().inner().clone() and adds it as a `db`\nfield on ApiState (server/state.rs), so axum handlers reach it the same way\nTauri commands do. db::with_conn() wraps axum-side access in\ntokio::task::spawn_blocking so a query never blocks an axum worker thread\nwhile holding the Mutex; #[tauri::command] handlers lock DbHandle directly\n(Tauri already runs command bodies off the async reactor thread).\n\nDeps added to src-tauri/Cargo.toml: rusqlite 0.40 (bundled feature -\nconfirmed via Context7/cargo info as the current release; PRD Appendix A\ncites 0.37, superseded), rusqlite_migration 2.6. Migrations are 6 M::up()\nentries (one per §4 entity) in db/migrations.rs, run via\nMigrations::to_latest() against user_version - idempotent by construction\n(a second run against an already-migrated DB just reads one pragma, no DDL\nre-executes). journal_mode=WAL and foreign_keys=ON are set once in\ndb::init() *before* to_latest() runs, per rusqlite_migration's own docs\n(\"PRAGMA statements are generally discouraged\" inside M::up).\n\nSchema decisions worth flagging for conceptify-qxr.1 (projects backend) and\nsibling CRUD beads:\n- All `id` columns are TEXT PRIMARY KEY with no generation scheme baked in\n  here - deliberately left to the CRUD beads (no uuid crate was added; add\n  whatever you need, e.g. `uuid` v4, when you write the first INSERT).\n- CHECK constraints added exactly where PRD §4 enumerates values in parens:\n  threads.status, comments.status, artifacts.created_by, and\n  follow_up_runs.mode. follow_up_runs.status has NO CHECK constraint - PRD\n  §4's table lists it as plain \"status\" without an enumerated value set\n  (unlike threads.status), so it's free-form TEXT; conceptify-b12.1 (agent\n  spawner) should define/document its actual value set (candidates per\n  FR-4.8: running/completed/failed/cancelled) when it starts writing rows.\n- comments has a COMPOSITE foreign key: FOREIGN KEY (thread_id,\n  artifact_version) REFERENCES artifacts(thread_id, version) ON DELETE\n  CASCADE. This enforces that a comment can only be inserted against an\n  artifact version that already exists in the artifacts table - insert the\n  artifacts row for a version before inserting comments anchored to it.\n  Verified this fires correctly (manual sqlite3 test, see below).\n- All parent-child relations use ON DELETE CASCADE (project -\u003e threads -\u003e\n  artifacts/comments/follow_up_runs), so deleting a project cascades\n  everything under it. Nothing currently exercises delete paths - just be\n  aware cascade is live once foreign_keys=ON (which db::init() sets on the\n  shared connection every launch).\n- Timestamps are TEXT, ISO-8601 UTC via strftime('%Y-%m-%dT%H:%M:%fZ',\n  'now') (sortable, includes fractional seconds), not SQLite's\n  CURRENT_TIMESTAMP.\n- settings is a plain key/value TEXT table (key PK, value TEXT), not fixed\n  columns - PRD §4 lists settings as free-form examples, not a schema.\n\nNew route: GET /api/v1/debug/db-check (authenticated) - documented in\ndocs/api.md. Demo #[tauri::command] db_check in lib.rs mirrors it for the\nfrontend side. Added a Rust-side IPC test (src-tauri/src/lib.rs\ntests::db_check_command_reads_through_managed_state, using\ntauri::test::mock_builder/get_ipc_response, gated by a `tauri` dev-dependency\nwith the \"test\" feature) since there's no headless way to poke the real\nWKWebView devtools in this environment (same limitation noted on bead\nconceptify-36s.2) - this exercises the actual IPC dispatch path against a\nthrowaway temp-file DB, not just a compile-time check. `cargo test` passes.\n\nVerification performed (all against a real running instance - see below for\nwhy no separate npm run tauri dev session was used for the HTTP parts):\n1. DB file exists at ~/Library/Application Support/conceptify/conceptify.db;\n   `sqlite3 ... \"PRAGMA journal_mode;\"` -\u003e wal. Confirmed (plus\n   conceptify.db-wal/-shm sidecars present).\n2. Idempotent relaunch: confirmed two ways - (a) a second, independent\n   process (my own `npm run tauri dev` launch) opened the same DB file and\n   ran db::init() again with no errors before its server logic deferred to\n   an already-running instance (proves setup() succeeded a second time\n   against an already-migrated DB); (b) `sqlite3 ... \"PRAGMA user_version;\"`\n   reads 6, matching the 6 defined migrations, stable across runs.\n3. All six tables present with correct columns via `sqlite3 ... \".schema\"`.\n   Constraint enforcement verified directly with transactional (rolled\n   back, non-destructive) INSERTs: root_path UNIQUE fires (\"UNIQUE\n   constraint failed: projects.root_path\"), threads.status CHECK fires on\n   an invalid value, and the FK on threads.project_id fires\n   (\"FOREIGN KEY constraint failed\") with foreign_keys=ON. No rows leaked\n   (verified count(*) = 0 afterward).\n4. Both paths verified live: `curl -H \"Authorization: Bearer $(cat\n   .../token)\" http://127.0.0.1:\u003cport\u003e/api/v1/debug/db-check` -\u003e\n   {\"ok\":true,\"project_count\":0}; and the `db_check` command was invoked\n   via tauri::test's real IPC dispatch in an automated test (see above),\n   returning 0.\n\nEnvironment note: while working this bead, a concurrent agent (CLI-skeleton\nwork) was actively running its own test cycles against a live Conceptify.app\ninstance built from this same working tree (same repo, no isolation) - that\ninstance ended up being the one used for steps 1/3/4's live curl check,\nsince my own dev launch correctly deferred to it (port-fallback/occupant-\ndetection working as designed). I did not kill that instance; I only killed\nprocesses I personally started (my own `npm run tauri dev` + child\ncargo-run'd app + vite, confirmed via ps/lsof afterward - no stray listeners\non 4477-4487 or dev servers left running that I started). Also confirmed:\nthe repo now has a root Cargo.toml (workspace: src-tauri +\ncrates/conceptify-types + crates/conceptify-cli), created by that same\nconcurrent agent - all work here stayed inside src-tauri/ + docs/api.md per\nthe task boundary; `cargo check`/`cargo test`/`cargo clippy --all-targets`\nall run clean from within src-tauri using the new workspace target/.","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:50Z","created_by":"Chris","updated_at":"2026-07-04T05:24:21Z","started_at":"2026-07-04T05:05:10Z","closed_at":"2026-07-04T05:24:21Z","close_reason":"rusqlite + WAL + 6 idempotent migrations implementing the full §4 schema with CHECK/FK constraints; DbHandle shared via Tauri managed state to both tauri commands and axum handlers; verified live + via cargo test","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.3","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:50Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.3","depends_on_id":"conceptify-36s.1","type":"blocks","created_at":"2026-07-01T22:40:46Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.2","title":"axum HTTP API server in Tauri setup hook (health, token, port files)","description":"Embed the local HTTP API (PRD §5.1): axum 0.8 spawned in the Tauri setup hook via tauri::async_runtime::spawn, bound to 127.0.0.1:4477. Share the AppHandle into axum state so handlers can emit Tauri events. Auth: random bearer token generated on first run, written mode 0600 to ~/Library/Application Support/conceptify/token; all endpoints except GET /health require it (S1). Port conflicts: on AddrInUse probe /health — if another Conceptify answers, defer to it; otherwise fall back through 4477-4487 and write the bound port to .../conceptify/port for CLI discovery. Routes live under /api/v1/ from day one (FR-8/§7.8).","design":"PRD §5.1, §7.8, §9 S1. Versioned API path from day one; keep a single OpenAPI-ish reference doc in-repo shared by CLI, skill, and app.","acceptance_criteria":"GET /health responds unauthenticated; any other route 401s without the bearer token; token file exists with 0600 perms; occupying 4477 with another process makes the app bind 4478 and update the port file; an axum handler can emit a Tauri event received by the webview.","notes":"Implementation notes (verified manually, see session transcript for full curl/log output):\n\nModule layout: src-tauri/src/server/{mod,state,paths,auth,net,routes}.rs, wired\nfrom lib.rs via `.setup(|app| { tauri::async_runtime::spawn(server::start(app.handle().clone())); Ok(()) })`.\n- state.rs: ApiState { app_handle: AppHandle, token: Arc\u003cstr\u003e } (Clone, used as axum State).\n- paths.rs: ~/Library/Application Support/conceptify/{token,port} via dirs::data_dir()\n  (deliberately NOT app.path().app_data_dir(), which would nest under the\n  com.chrismeehan.conceptify bundle id — CLI needs a fixed path with no Tauri context).\n- auth.rs: 32 random bytes (rand 0.9, rand::rng()) hex-encoded, temp+rename write,\n  chmod 0600 (also re-asserted on reuse). Reused across restarts (loaded if present).\n  require_bearer_token is an axum::middleware::from_fn_with_state layer.\n- net.rs: bind_with_fallback() loops 4477..=4487; on AddrInUse probes the occupant's\n  /health with a raw TCP GET + 750ms timeout, looking for the substring\n  \"\\\"service\\\":\\\"conceptify\\\"\" in the body to distinguish a real Conceptify instance\n  from an unrelated process. Match -\u003e BindOutcome::DeferToExisting (logs, does not\n  bind anywhere, does not touch the port file). No match -\u003e tries next port. All\n  ports exhausted -\u003e BindOutcome::NoPortAvailable (logs, server simply doesn't start;\n  never panics/crashes per N4).\n- routes.rs: root GET /health + /api/v1/health both unauthenticated (mirrors both\n  shapes mentioned in the bead text); /api/v1/ping is the demo authenticated route\n  AND doubles as the event-emission demo (emits \"api-ping\" via AppHandle::emit,\n  logged server-side). Frontend listener added in src/App.tsx (useEffect + listen()\n  from @tauri-apps/api/event) logging to console.\n- docs/api.md: auth scheme, discovery/port-fallback behavior, versioning rule,\n  events table, /health and /api/v1/ping reference. Extend this doc, don't fork a\n  second one, as later beads add routes.\n\nVerification performed (npm run tauri dev, then killed; also ran the built\ntarget/debug/conceptify binary directly for the two-instance defer test):\n1. curl /health and /api/v1/health -\u003e 200 unauthenticated. Confirmed.\n2. curl /api/v1/ping without token -\u003e 401; with token from token file -\u003e 200\n   {\"pong\":true}; with wrong token -\u003e 401. Confirmed.\n3. stat -f \"%Lp\" token -\u003e 600. Confirmed.\n4. Occupied 4477 with `python3 -m http.server`, relaunched app -\u003e logs show\n   \"port 4477 occupied by an unrelated process; trying next port\" -\u003e\n   \"listening on 127.0.0.1:4478\"; port file contained \"4478\";\n   curl 127.0.0.1:4478/health -\u003e 200. Confirmed.\n5. Additionally verified the DeferToExisting branch directly: ran the compiled\n   binary twice back-to-back (A binds 4477; B hits AddrInUse, probes, sees\n   \"service\":\"conceptify\" in A's /health body, logs\n   \"deferring to it\" and does NOT bind 4478); confirmed via lsof only one\n   listener existed and the port file still read 4477.\n   Event emission: server-side log confirms app_handle.emit(\"api-ping\", ...)\n   returns Ok on every /api/v1/ping call. Frontend listener code added,\n   type-checks and the vite/tsc build passes. Could not visually confirm the\n   console.log firing inside WKWebView's inspector in this headless run (no\n   GUI/devtools automation tool available in this environment) - this is the\n   one criterion only partially confirmed end-to-end; recommend a quick manual\n   check (Safari \u003e Develop \u003e open the running app's inspector, hit\n   /api/v1/ping, watch the console) before fully trusting the wire-up, though\n   the code path is standard `@tauri-apps/api/event` usage and the emit side\n   is proven to execute without error.\n\nAll test processes (python http.server, direct binary launches, npm run tauri\ndev) were killed after verification; ports 4477-4487 confirmed free afterward.\n\nGotchas for downstream beads:\n- 36s.3 (SQLite): rusqlite connection should go into Tauri managed state\n  (`app.manage(...)`) in the same setup hook, then be added as a field on\n  ApiState (or a second State extractor) so axum handlers can reach it -\n  ApiState is intentionally a plain struct, not wrapped in Arc itself, so\n  adding fields is a one-line change; just remember DB access from axum\n  handlers needs a Mutex/pool since ApiState is cloned per request.\n- 36s.5 (CLI): read the token from ~/Library/Application Support/conceptify/token\n  and the port from .../port (plain integer, no newline handling needed but\n  trim anyway defensively). The CLI's liveness probe should hit GET /health\n  (unauthenticated, no token needed) at the discovered port before doing\n  anything else, per the launch-and-wait contract in PRD §5.2. Note port file\n  is NOT deleted/reset between runs - if the app is not currently running, the\n  file may contain a stale port from a previous session; the CLI must still\n  verify liveness via /health rather than trusting the file blindly.\n- Both: docs/api.md is the shared reference - add sections there rather than\n  writing new docs.","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:49Z","created_by":"Chris","updated_at":"2026-07-02T06:22:44Z","started_at":"2026-07-02T06:07:52Z","closed_at":"2026-07-02T06:22:44Z","close_reason":"axum 0.8 API embedded in Tauri setup hook: /health unauthenticated, bearer-token auth (0600 token file), port fallback 4477-4487 with defer-to-existing-instance, port file, event emission wired; all acceptance criteria verified","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.2","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:49Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.2","depends_on_id":"conceptify-36s.1","type":"blocks","created_at":"2026-07-01T22:40:45Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.1","title":"Tauri v2 app scaffold: Preact + Tailwind v4 + Vite","description":"Create the app skeleton (PRD §5.3, Appendix A). Scaffold with create-tauri-app (React template, since the official Preact template was removed in create-tauri-app v3), then swap React → Preact via @preact/preset-vite, and add Tailwind v4 via @tailwindcss/vite. Verify dev loop (hot reload) and release build work. This is the root of the repo — everything else hangs off it.","design":"Tauri 2.x stable line (2.11.x at research time). macOS webview is system WKWebView — author Safari-compatible frontend code from day one (Appendix A WKWebView gotchas).","acceptance_criteria":"just-cloned repo: dev command opens the Tauri window with a Preact component styled by Tailwind; release build produces a launchable .app; cold start well under 1s (N1).","status":"closed","priority":1,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:48Z","created_by":"Chris","updated_at":"2026-07-02T06:07:51Z","started_at":"2026-07-02T05:57:13Z","closed_at":"2026-07-02T06:07:51Z","close_reason":"Scaffold complete: Tauri 2.11 + Preact + Tailwind v4 (stable) + Vite; dev loop and release .app bundle verified","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.1","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:48Z","created_by":"Chris","metadata":"{}"}],"dependency_count":0,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"conceptify-959","title":"M6 — Ask-from-app, project creation, settings polish (self-sufficiency)","description":"Milestone 6 (PRD §11 M6): Conceptify becomes self-sufficient — no coding session required to ask questions. Covers the in-app question composer with streaming progress (FR-5.x), error/timeout/retry paths, in-app project creation with dir picker/auto-create (FR-1.2), the settings UI (FR-7.x), an end-to-end QA pass of all six use cases, and README + setup script. Completing this epic completes Phase 1.","acceptance_criteria":"All child tasks closed; QA pass confirms UC1-UC6 all work; README and setup script let a fresh machine (or fresh clone) reach a working install.","status":"open","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:46Z","created_by":"Chris","updated_at":"2026-07-02T05:39:46Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959","depends_on_id":"conceptify-b12","type":"blocks","created_at":"2026-07-01T22:40:43Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12","title":"M5 — Follow-up loop (the interrogation engine)","description":"Milestone 5 (PRD §11 M5): the second half of the crown jewel — comments get answered by background agents. Covers the agent adapter + per-purpose model config (§5.5), the headless spawner (process mgmt, streaming, timeout, cancel, logs), get-context/resolve-comment API+CLI, the Ask-follow-ups batch flow (FR-4.6, sidebar-first), the Apply-to-artifact flow (FR-4.7, new version + live refresh), skill follow-up guidance (FR-6.4), and run status UI. Checkpoint: UC4 end-to-end — comment on a diagram node, get a sidebar answer, apply a clarification, watch the artifact update live.","acceptance_criteria":"All child tasks closed; M5 checkpoint task passes: UC4 works end-to-end including live artifact update and comment transitioning to 'applied'.","status":"open","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:46Z","created_by":"Chris","updated_at":"2026-07-02T05:39:46Z","labels":["m5","phase1"],"dependencies":[{"issue_id":"conceptify-b12","depends_on_id":"conceptify-57l","type":"blocks","created_at":"2026-07-01T22:40:42Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12","depends_on_id":"conceptify-94m","type":"blocks","created_at":"2026-07-01T22:40:43Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-94m","title":"M4 — Comments (annotation layer)","description":"Milestone 4 (PRD §11 M4): the first half of the crown-jewel interrogation loop (G5, §7.4). Covers the bridge script + server-side injection, text-selection comments with W3C-style anchors, diagram-element comments via data-cfy-id, the direct follow-up composer, the comments sidebar (filters, statuses, scroll-to-anchor, persistent highlights), and anchor re-attachment across artifact versions with 'reference moved' handling. Demoable outcome: highlight text or click a diagram node, leave a comment, see it anchored and highlighted.","acceptance_criteria":"All child tasks closed; text and diagram comments can be created, listed, filtered, and clicked-to-scroll; anchors survive artifact version switches or degrade to 'reference moved' — never silently dropped.","status":"closed","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:45Z","created_by":"Chris","updated_at":"2026-07-05T00:00:24Z","closed_at":"2026-07-05T00:00:24Z","close_reason":"Closed","labels":["m4","phase1"],"dependencies":[{"issue_id":"conceptify-94m","depends_on_id":"conceptify-nsy","type":"blocks","created_at":"2026-07-01T22:40:41Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-57l","title":"M3 — Skill v1: agent → app end-to-end (authoring integration)","description":"Milestone 3 (PRD §11 M3): the Claude Code skill (§7.6) makes 'use conceptify to explain X' work from any repo. Covers the skill flow (FR-6.1), authoring guide with design scaffold + tiered visual strategy and exact render commands (FR-6.2, §8), the self-review loop (FR-6.3), conceptify doctor (FR-6.5), and quality iteration on real repos. Checkpoint: UC1/UC2/UC3 work start-to-finish from a cold app.","acceptance_criteria":"All child tasks closed; M3 checkpoint task passes: UC1 (explain from session), UC2 (syntax question), UC3 (codebase tour) all work from a cold app with consistently good artifact quality.","status":"open","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:44Z","created_by":"Chris","updated_at":"2026-07-02T05:39:44Z","labels":["m3","phase1"],"dependencies":[{"issue_id":"conceptify-57l","depends_on_id":"conceptify-nsy","type":"blocks","created_at":"2026-07-01T22:40:40Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy","title":"M2 — Artifact pipeline \u0026 reading experience (the viewer)","description":"Milestone 2 (PRD §11 M2): artifacts flow in and render beautifully. Covers the artifact:// protocol handler with per-response CSP, the sandboxed-iframe viewer, the save-artifact endpoint (validation, versioning, atomic writes, live refresh), the design-system CSS scaffold (D1-D6), the artifact spec doc (§7.3), version switcher, and open-in-browser. Checkpoint: a hand-written test artifact renders beautifully in-app AND in Safari/Chrome — the portability guarantee (G3).","acceptance_criteria":"All child tasks closed; M2 checkpoint task passes: hand-written test artifact renders correctly in-app and standalone in Safari and Chrome.","status":"closed","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:43Z","created_by":"Chris","updated_at":"2026-07-04T22:35:57Z","closed_at":"2026-07-04T22:35:57Z","close_reason":"Closed","labels":["m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy","depends_on_id":"conceptify-qxr","type":"blocks","created_at":"2026-07-01T22:40:40Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-qxr","title":"M1 — Projects \u0026 threads (core data + shell UI)","description":"Milestone 1 (PRD §11 M1): the core domain objects become real. Projects (ensure/create/list/rename/archive) and threads (create/list) exposed through API + CLI + UI, app shell navigation (project sidebar → thread list), and Tauri event wiring so lists update live. Demoable outcome: create a project and thread from the CLI and watch them appear in the app without a refresh.","acceptance_criteria":"All child tasks closed; projects and threads manageable via API, CLI, and UI; list views update live via Tauri events.","status":"closed","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:43Z","created_by":"Chris","updated_at":"2026-07-04T21:03:03Z","closed_at":"2026-07-04T21:03:03Z","close_reason":"Closed","labels":["m1","phase1"],"dependencies":[{"issue_id":"conceptify-qxr","depends_on_id":"conceptify-36s","type":"blocks","created_at":"2026-07-01T22:40:39Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-36s","title":"M0 — Foundations (scaffolding)","description":"Milestone 0 of Conceptify (PRD §11 M0): stand up the Tauri v2 app skeleton so every later milestone has rails to build on. Covers the app scaffold (Preact + Tailwind v4 + Vite), the axum HTTP API with token/port files, the rusqlite database with the §4 schema, app lifecycle behavior (single-instance, hide-on-close, window-state), the conceptify CLI skeleton with its launch-and-wait contract, and the justfile. Demoable outcome: app launches fast, /health answers, CLI can reach the API, DB schema migrated.","acceptance_criteria":"All child tasks closed; app cold-launches, GET /health responds, CLI probe-launch-poll contract works against a cold app, schema migrated in WAL-mode SQLite.","status":"closed","priority":1,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:42Z","created_by":"Chris","updated_at":"2026-07-04T05:34:34Z","closed_at":"2026-07-04T05:34:34Z","close_reason":"All 7 children closed: app cold-launches with axum API + WAL SQLite, CLI probe-launch-poll contract verified E2E, lifecycle plugins wired, justfile entry points working","labels":["m0","phase1"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-iho","title":"Migration: extend follow_up_runs.mode CHECK with 'ask' (in-app ask runs)","description":"The shipped follow_up_runs DDL constrains mode with CHECK (mode IN ('answer','apply')), so the in-app ask flow (conceptify-959.1) cannot record its runs as mode='ask' without a schema change. Decision recorded on conceptify-b12.2: in-app asks SHOULD be follow_up_runs rows (third mode 'ask' -\u003e Purpose::InAppAsk) so they get the same log/status/guard machinery. SQLite cannot ALTER a CHECK constraint, so this needs an appended migration that rebuilds the table (create new table with CHECK (mode IN ('answer','apply','ask')), INSERT INTO ... SELECT, drop old, rename, recreate idx_follow_up_runs_thread_id) per the append-only contract in src-tauri/src/db/migrations.rs. Engine change is then trivial: add RunMode::Ask in src-tauri/src/runs.rs (as_str 'ask', purpose InAppAsk).","status":"open","priority":2,"issue_type":"task","owner":"cdcmeehan@gmail.com","created_at":"2026-07-04T23:58:01Z","created_by":"Chris","updated_at":"2026-07-04T23:58:01Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-nsy.8","title":"Inline \u003ccode\u003e overflows horizontally at narrow pane widths (no overflow-wrap)","description":"Found during the M2 checkpoint (nsy.7) end-to-end pass. At reading-pane widths below ~458px the artifact overflows horizontally (document.documentElement.scrollWidth 458 \u003e clientWidth), producing a page-level horizontal scrollbar / cut-off content. Root cause: inline \u003ccode\u003e in prose has overflow-wrap:normal (design-system.css line ~481, selector 'p code, li code, td code, th code, figcaption code, summary code, dd code'). A single unbreakable token in the demo — 'artifact://thread/\u003cslug\u003e/artifact.v3.html' (42 chars, inside a step \u003cli\u003e) — refuses to break and sets a fixed ~458px minimum page width. Everything else scales/clips correctly: \u003cpre\u003e blocks scroll internally (overflow-x:auto, verified rectW 240 / scrollW 549), tables use .cfy-table-wrap (overflow-x:auto), SVGs honor max-width:100% (both rendered at 190px in a 288px column). Where it bites in-app: the default 800px window minus sidebar (w-56=224) + thread list (w-72=288) leaves a 288px reading pane, so it shows on first run. Mitigated after one resize by tauri-plugin-window-state persistence (PRD line 105). At \u003e=460px pane the artifact is flawless (verified gorgeous at 640/900px in Chrome + WebKit, light+dark).","design":"Add 'overflow-wrap: break-word;' to the inline-code selector in skill/design-system.css (~line 481). break-word only breaks a token when it would otherwise overflow, so normal-width rendering is unchanged, and it is inert on 'pre code' (white-space:pre) so block-code horizontal scroll is unaffected. IMPORTANT: skill/examples/demo-artifact.html embeds the scaffold VERBATIM as its first \u003cstyle\u003e block, so make the identical edit there too (keep them byte-in-sync) and re-run 'just install-skill' to refresh the installed snapshot. Optional/related (product call, not required here): widen the initial window in src-tauri/tauri.conf.json (currently 800x600) so the first-run reading pane isn't 288px. Boundary note: nsy.7 left design-system.css untouched because it is a closed-bead (nsy.5) deliverable and a parallel agent was active; acceptance permits filing.","acceptance_criteria":"At a 288px viewport the demo artifact has document.documentElement.scrollWidth == clientWidth (no horizontal page overflow); long inline-code tokens wrap; normal/wide-width rendering is visually unchanged; skill/design-system.css and the embedded copy in skill/examples/demo-artifact.html stay identical; installed skill re-snapshotted.","status":"closed","priority":2,"issue_type":"task","owner":"cdcmeehan@gmail.com","created_at":"2026-07-04T22:32:27Z","created_by":"Chris","updated_at":"2026-07-04T22:35:56Z","closed_at":"2026-07-04T22:35:56Z","close_reason":"Fixed: overflow-wrap:break-word on inline code in scaffold + demo embedded copy; skill reinstalled","labels":["design","m2","phase1"],"dependencies":[{"issue_id":"conceptify-nsy.8","depends_on_id":"conceptify-nsy","type":"parent-child","created_at":"2026-07-04T15:32:26Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-nsy.8","depends_on_id":"conceptify-nsy.7","type":"discovered-from","created_at":"2026-07-04T15:32:27Z","created_by":"Chris","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-bvr","title":"Skill: consider a post-process helper script for generated SVG (id stamping + style stripping)","description":"rendering.md currently instructs the authoring agent to post-process d2/dot SVG by hand (strip prolog/styles/ids, base64-decode d2 group classes, derive+stamp data-cfy-id per spec §4.4). The recipe is deterministic, so a bundled scripts/postprocess-svg.mjs (mirroring scripts/highlight.mjs) would cut per-artifact effort and eliminate derivation mistakes. Decide during 57l.4 quality iteration whether hand-editing error rates justify shipping it.","notes":"BUILT (decision pulled forward from 57l.4 per orchestrator: deterministic recipe, clearly cheap, kills derivation variance): skill/scripts/postprocess-svg.mjs — zero-dep node script mirroring highlight.mjs style. Implements rendering.md + artifact-spec §4.4 exactly: d2 (strip prolog, delete both \u003cstyle\u003e blocks, strict-base64-decode group classes with round-trip validation so real classes like 'text' can't false-positive, HTML-unescape, drop [0] / append -{n+1} for parallel edges [n\u003e=1], stamp data-cfy-id namespaced under --fig) and dot (strip prolog/DOCTYPE/comments, remove positional id='graph0|nodeN|edgeN|clustN' attrs, entity-decode \u003cg class='node|edge|cluster'\u003e titles, stamp; root class='graph' skipped — the \u003cfigure\u003e carries the fig id). Collisions get -2/-3 in document order seeded from ALL pre-existing data-cfy-ids, so re-runs and partial stamps stay stable. Deterministic + idempotent (verified: second run byte-identical on d2 flow/sequence/parallel-edge/container and dot cluster/parallel-edge outputs). stdin and --input/--write modes verified equal; async stdin read because fs.readFileSync(0) EAGAINs on macOS when the piping renderer is slow (latent footgun that also exists in highlight.mjs's fast-pipe usage). Mermaid SVG detected and rejected exit 2 per OQ5. Verification: 34 stamped ids across 4 real renders all pass the §4.2 regex, 0 dups; processed d2+dot SVGs spliced into a minimal artifact passed a scratch checker implementing spec §8 rules (W-ID-FORMAT/W-ID-DUP/W-ANCHOR-DIAGRAM/W-ANCHOR-HEADINGS/W-SRC-MALFORMED/W-SRC-ORPHAN/E-EXTERNAL-CODE positions) — checked against the spec by construction rather than the Rust validator because src-tauri was owned by a parallel agent. rendering.md now shows the script as primary path in the d2 and dot sections; the old hand-recipe is kept as 'Manual fallback / reference'; SKILL.md got a one-line pointer. Known quirk, acceptable: d2 sequence diagrams emit two groups per actor (box + lifeline span), so the second deterministically gets '\u003cactor\u003e-2'.","status":"closed","priority":2,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-04T22:00:16Z","created_by":"Chris","updated_at":"2026-07-04T23:05:42Z","started_at":"2026-07-04T22:48:57Z","closed_at":"2026-07-04T23:05:42Z","close_reason":"Closed","dependencies":[{"issue_id":"conceptify-bvr","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-04T15:00:24Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-959.6","title":"README + setup script","description":"Make the repo self-installing (PRD §11 M6): README covering what Conceptify is, architecture sketch, prerequisites (d2, graphviz, node, claude), and setup; a setup script (driving the justfile) that builds the app, installs the CLI onto PATH, installs the skill to ~/.claude/skills/conceptify/, and suggests the .conceptify/ global-gitignore entry; points at conceptify doctor for verification.","acceptance_criteria":"On a fresh clone: one documented command sequence yields a working app + CLI + skill; doctor passes; README accurate to what shipped.","status":"open","priority":2,"issue_type":"task","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:39Z","created_by":"Chris","updated_at":"2026-07-02T05:40:39Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.6","depends_on_id":"conceptify-36s.6","type":"blocks","created_at":"2026-07-01T22:41:44Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.6","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:45Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.6","depends_on_id":"conceptify-57l.3","type":"blocks","created_at":"2026-07-01T22:41:45Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.6","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:38Z","created_by":"Chris","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-959.4","title":"Settings UI (FR-7.1 – FR-7.4)","description":"Surface configuration (PRD §7.7): agent adapters — default adapter, command templates, per-purpose models (followUp/artifactUpdate/inAppAsk), timeout (FR-7.1); appearance light/dark/system (FR-7.2); paths — auto-project base dir, agent binary override (FR-7.3). Stored in SQLite via the settings model; sensible defaults mean the app works with ZERO configuration when claude is on PATH (FR-7.4) — settings UI is for overrides, not onboarding.","acceptance_criteria":"Every §7.7 setting is editable and takes effect without restart where feasible; resetting to defaults works; invalid command templates rejected with a helpful message; fresh install runs correctly having never opened settings.","status":"open","priority":2,"issue_type":"feature","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:36Z","created_by":"Chris","updated_at":"2026-07-02T05:40:36Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.4","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:36Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.4","depends_on_id":"conceptify-b12.1","type":"blocks","created_at":"2026-07-01T22:41:38Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.4","depends_on_id":"conceptify-qxr.4","type":"blocks","created_at":"2026-07-01T22:41:39Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-959.3","title":"In-app project creation: dir picker + auto-create (FR-1.2)","description":"UC6 (PRD FR-1.2): create a project from the app — native directory picker for existing dirs (repos), or 'create a folder for me' → ~/Documents/conceptify/projects/\u003cslugified-name\u003e/ for non-codebase topics (e.g. 'distributed systems'). Auto-create base dir configurable (FR-7.3). Reuses the ensure-project canonicalization path.","acceptance_criteria":"Both flows create a usable project; auto-created folder exists at the configured base; picking an already-mapped directory lands on the existing project instead of erroring.","status":"open","priority":2,"issue_type":"feature","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:35Z","created_by":"Chris","updated_at":"2026-07-02T05:40:35Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.3","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:35Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.3","depends_on_id":"conceptify-qxr.1","type":"blocks","created_at":"2026-07-01T22:41:37Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.3","depends_on_id":"conceptify-qxr.4","type":"blocks","created_at":"2026-07-01T22:41:37Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-959.2","title":"Error, timeout, and retry paths (FR-5.3)","description":"Failure hygiene for in-app asks (PRD FR-5.3): agent crash or timeout (default 15 min, configurable) → thread status 'error', log viewable from the thread, one-click retry (re-spawns with the same question into the same thread). Applies the N4 rule: a failed run never corrupts thread state.","acceptance_criteria":"Simulated crash and timeout both land the thread in 'error' with the log accessible; retry succeeds and the thread proceeds to ready; no orphaned processes or stuck 'generating' states.","status":"open","priority":2,"issue_type":"feature","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:34Z","created_by":"Chris","updated_at":"2026-07-02T05:40:34Z","labels":["m6","phase1"],"dependencies":[{"issue_id":"conceptify-959.2","depends_on_id":"conceptify-959","type":"parent-child","created_at":"2026-07-01T22:40:34Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-959.2","depends_on_id":"conceptify-959.1","type":"blocks","created_at":"2026-07-01T22:41:36Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-b12.8","title":"OQ3: permission scoping for headless runs","description":"Open question 3 (PRD §12): headless runs currently use --permission-mode acceptEdits — scoped how? Artifact-apply runs only need to write within the thread folder. Investigate per-adapter sandboxing/allowed-tools flags (Claude Code allowed-tools, permission modes; whatever Codex offers later) during M5 and pick the tightest workable scoping. Right-sized security (§9): containment and hygiene, not adversarial defense.","acceptance_criteria":"Decision recorded; adapter default args updated to the chosen scoping; documented in the skill/README so future adapters follow the same principle.","status":"open","priority":2,"issue_type":"decision","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:31Z","created_by":"Chris","updated_at":"2026-07-02T05:40:31Z","labels":["m5","open-question","phase1"],"dependencies":[{"issue_id":"conceptify-b12.8","depends_on_id":"conceptify-b12","type":"parent-child","created_at":"2026-07-01T22:40:30Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-b12.8","depends_on_id":"conceptify-b12.2","type":"blocks","created_at":"2026-07-01T22:41:31Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.6","title":"OQ4: confirm early thread creation (generating status) feels right","description":"Open question 4 (PRD §12): current spec creates the thread BEFORE the artifact exists (UC1 shows a 'generating' thread early — better UX) with error status + retry on agent failure, vs. only registering threads on save-artifact (no orphan threads). Confirm the chosen behavior feels right during M3 real-repo iteration; if orphaned error-threads become noise, revisit. Record the confirmation or the change.","acceptance_criteria":"Decision confirmed or reversed based on M3 experience; thread-creation flow and skill updated if changed.","status":"in_progress","priority":2,"issue_type":"decision","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:14Z","created_by":"Chris","updated_at":"2026-07-04T23:40:06Z","started_at":"2026-07-04T23:40:06Z","labels":["m3","open-question","phase1"],"dependencies":[{"issue_id":"conceptify-57l.6","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:14Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.6","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:12Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.5","title":"OQ5: is Mermaid pre-rendering worth its weight in the skill?","description":"Open question 5 (PRD §12): decide empirically during M3 iteration whether Mermaid pre-rendering (mermaid-cli/Puppeteer toolchain) earns its place in the skill, or whether D2 + Graphviz DOT + hand-authored SVG covers all structured-diagram needs (§8 Tier 1). Weigh: toolchain weight and flakiness vs. cases where Mermaid's syntax reliability wins. Record the decision and update the skill's visual-strategy section accordingly.","acceptance_criteria":"Decision recorded with evidence from M3 iterations; skill guide updated to match (either exact mermaid render commands, or explicit 'use D2/DOT instead' guidance).","notes":"OQ5 DECIDED (empirically, 2026-07-04): Mermaid pre-rendering is NOT in the skill toolchain; D2 + Graphviz DOT + hand-authored SVG stay the complete Tier-1 set. Evidence (mmdc 11.16.0 via npx, this machine): (1) WEIGHT — first npx run ~70s and downloaded full Chrome + chrome-headless-shell (~550MB) into ~/.cache/puppeteer; every render boots headless Chrome (~3.5s/diagram vs ~0.3s for d2; d2/dot need no network ever). (2) THEMING FRAGILITY — themeVariables are silently ignored unless theme:'base' (verified: sentinels landed 0x with theme:'neutral', correctly with 'base'); neo/handDrawn sequence actors bake literal #eaeaea/#666 from variables outside the D5 token table (actorBkg etc.), so staying on-system needs per-diagram-type variable maps; handDrawn roughjs paths 4x the file size (16KB-\u003e64KB for a 5-node flowchart). (3) ANCHORABILITY (decisive) — state-diagram edges emit only positional ids (edge0, edge1) which artifact-spec §4.2 forbids; sequence messages have no bounding \u003cg\u003e grouping line+label; every mmdc SVG is id='my-svg' with #my-svg-scoped \u003cstyle\u003e and url(#my-svg-*) markers, colliding when an artifact has 2+ diagrams; flowchart/state labels are HTML-in-foreignObject. Flowchart node/edge ids ARE semantic (flowchart-client-0, L_client_middleware_0), but flowcharts are exactly where d2 already wins. (4) WHAT WE FORGO — denser sequence diagrams (notes/activations) and gantt/ER/gitgraph/journey. Mitigations: d2 sequence output is clean (labels knocked out via SVG mask — apparent overlap in rsvg thumbnails is a converter artifact, browsers render correctly); gantt/ER in an explanation artifact are usually better as cfy-table/cfy-steps; genuinely interactive cases keep Tier-2 runtime mermaid (pinned CDN + pre fallback), unchanged. REVISIT IF: 57l.4 shows d2 sequence quality failing on real content; artifacts repeatedly need gantt/ER at generation time; or a browser-free mermaid renderer ships. rendering.md updated with a 'Mermaid pre-rendering — NOT in the toolchain (OQ5)' section; scripts/postprocess-svg.mjs rejects mermaid SVG with a pointer to it. ELK note for the record: layout:'elk' does work in mmdc (verified vs dagre), irrelevant given the decision.","status":"closed","priority":2,"issue_type":"decision","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:13Z","created_by":"Chris","updated_at":"2026-07-04T23:05:41Z","started_at":"2026-07-04T22:48:56Z","closed_at":"2026-07-04T23:05:41Z","close_reason":"Closed","labels":["m3","open-question","phase1"],"dependencies":[{"issue_id":"conceptify-57l.5","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:12Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.5","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:12Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.3","title":"conceptify doctor: prerequisites check (FR-6.5)","description":"'conceptify doctor' CLI command (PRD FR-6.5): checks app installed, CLI on PATH, d2 present, graphviz (dot) present, node present (for Shiki), agent binary resolvable (login-shell lookup per §5.1); prints actionable per-check pass/fail with install hints. Skill docs reference it as the first debugging step; prerequisites also documented in the repo.","acceptance_criteria":"Doctor reports each dependency with clear pass/fail and remediation hint; exits non-zero when anything required is missing; documented in README/skill.","status":"closed","priority":2,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:11Z","created_by":"Chris","updated_at":"2026-07-04T22:42:15Z","started_at":"2026-07-04T22:37:05Z","closed_at":"2026-07-04T22:42:15Z","close_reason":"Closed","labels":["m3","phase1"],"dependencies":[{"issue_id":"conceptify-57l.3","depends_on_id":"conceptify-36s.5","type":"blocks","created_at":"2026-07-01T22:41:08Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.3","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:10Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"conceptify-57l.2","title":"Skill self-review loop (FR-6.3)","description":"De-risk the highest-variance tier — hand-authored SVG (PRD FR-6.3): before save-artifact, the skill instructs the agent to render the HTML (headless browser/screenshot) and review for overlapping labels, broken layout, and contrast issues, iterating until clean. Define the exact mechanism (which headless renderer, what the review checklist is) and bake it into the skill.","acceptance_criteria":"Skill includes a working, copy-pasteable self-review procedure; demonstrated catching at least one real layout defect (e.g. overlapping SVG labels) before publish during M3 iteration.","notes":"FR-6.3 visual self-review loop (57l.2) — mechanism + verification:\n\nMECHANISM: agent-browser (bundled headless Chromium, CDP) — the proven path from nsy.5/57l.1. Recipe: open file://\u003cartifact\u003e, then for each of 2 widths x 2 modes: 'set viewport \u003cw\u003e 900 2' + 'set media light|dark' + 'screenshot --full \u003cpng\u003e', then Read the PNGs and judge against a checklist. Widths 460 (narrow thread-pane / overflow-risk) and 900 (comfortable); deviceScaleFactor 2 for crisp text. set viewport/set media apply live to the next screenshot — no reload needed (verified). Rejected alternatives: app-window screencapture (TCC-blocked, per prior beads), Playwright-via-npx (not installed here — confirmed unreliable; kept only as a best-effort fallback note).\n\nMECHANICAL OVERFLOW TRIPWIRE (novel, verified): a '--full' PNG's pixelWidth == viewport_width x scale EXACTLY when there is no horizontal overflow (920 at 460/scale2; 1800 at 900/scale2). Anything wider = horizontal overflow at that width. 'sips -g pixelWidth' gives a cheap deterministic narrow-pane-overflow detector to pair with the visual read.\n\nANIMATION NOTE: headless Chromium runs animations to completion, so it CANNOT reproduce the WKWebView occlusion-suspension from-state trap — that stays a SOURCE check (scaffold ban, restated in checklist). Documented explicitly.\n\nDELIVERABLES: new skill/references/self-review.md (recipe + mechanical check + 8-item checklist + loop + fallback); SKILL.md step 5 rewritten from placeholder to Source review + Visual review (points at self-review.md), and self-review.md added to the 'Files in this skill' list. install-skill picks it up automatically (cp -R skill/.) — no justfile change. Reinstalled; verified present in ~/.claude/skills/conceptify/.\n\nVERIFIED E2E: golden fixture skill/examples/demo-artifact.html rendered clean on all 4 frames (widths match exactly: 920/920/1800/1800). Sabotaged scratchpad copy with 3 injected defects all caught: (1) overlapping SVG labels (moved fig-serve-path handler label x 350-\u003e150) visible in every frame; (2) hardcoded #211c15 fill on the hand-sketch 'opaque-origin iframe' label — readable in light, INVISIBLE in dark (dark-mode contrast break); (3) white-space:nowrap long command -\u003e narrow-frame pixelWidth 1594 (\u003e920) = horizontal overflow at 460px, exactly 1800 (clean) at 900px. Screenshots under scratchpad/57l2/.","status":"closed","priority":2,"issue_type":"feature","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:40:10Z","created_by":"Chris","updated_at":"2026-07-04T22:48:55Z","started_at":"2026-07-04T22:37:04Z","closed_at":"2026-07-04T22:48:55Z","close_reason":"Closed","labels":["m3","phase1"],"dependencies":[{"issue_id":"conceptify-57l.2","depends_on_id":"conceptify-57l","type":"parent-child","created_at":"2026-07-01T22:40:09Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-57l.2","depends_on_id":"conceptify-57l.1","type":"blocks","created_at":"2026-07-01T22:41:07Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.6","title":"justfile: dev / build / install-cli","description":"Developer entry points (PRD §11 M0): a justfile with at minimum 'just dev' (app dev loop), 'just build' (release build of app + CLI), and 'just install-cli' (build the CLI and symlink it into /usr/local/bin or ~/.local/bin so it's on PATH for agents). Keep it the single place build/install knowledge lives — the M6 setup script and README will call into it.","acceptance_criteria":"just dev / just build / just install-cli each work from a clean checkout; after install-cli, 'conceptify status' resolves from a fresh shell.","status":"closed","priority":2,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:53Z","created_by":"Chris","updated_at":"2026-07-04T05:33:11Z","started_at":"2026-07-04T05:26:48Z","closed_at":"2026-07-04T05:33:11Z","close_reason":"justfile with dev/build/install-cli (+check) verified from clean state; install-cli symlinks to ~/.local/bin, fresh-shell 'conceptify status' works","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.6","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:53Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.6","depends_on_id":"conceptify-36s.1","type":"blocks","created_at":"2026-07-01T22:40:48Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.6","depends_on_id":"conceptify-36s.5","type":"blocks","created_at":"2026-07-01T22:40:48Z","created_by":"Chris","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"conceptify-36s.4","title":"App lifecycle: single-instance, hide-on-close, window-state","description":"Lifecycle behavior (PRD §5.1): tauri-plugin-single-instance as duplicate-launch guard; window close HIDES instead of quitting (CloseRequested + prevent_close) so the HTTP API stays up while no window is visible; explicit Quit from the app menu; tauri-plugin-window-state for size/position persistence. This is what makes the CLI liveness contract cheap — the app being 'running' doesn't require a visible window.","design":"PRD §5.1 Lifecycle. Menu-bar/tray mode is Phase 2 — plain hidden window is enough for Phase 1.","acceptance_criteria":"Launching the app twice focuses the existing instance instead of starting a second; closing the window leaves the process alive and /health responding; reopening (e.g. via open -a) restores the window at its previous size/position; menu Quit actually exits.","status":"closed","priority":2,"issue_type":"task","assignee":"Chris","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:51Z","created_by":"Chris","updated_at":"2026-07-04T05:34:18Z","started_at":"2026-07-04T05:26:48Z","closed_at":"2026-07-04T05:34:18Z","close_reason":"single-instance (registered first), hide-on-close via CloseRequested+prevent_close, macOS Reopen restore, window-state plugin; default menu Quit exits (ExitRequested not intercepted); verified live where headless allows","labels":["m0","phase1"],"dependencies":[{"issue_id":"conceptify-36s.4","depends_on_id":"conceptify-36s","type":"parent-child","created_at":"2026-07-01T22:39:51Z","created_by":"Chris","metadata":"{}"},{"issue_id":"conceptify-36s.4","depends_on_id":"conceptify-36s.1","type":"blocks","created_at":"2026-07-01T22:40:46Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"conceptify-qmy","title":"Phase 2 — depth (backlog placeholder)","description":"Backlog epic capturing PRD §11 Phase 2 scope so it isn't lost. NOT part of Phase 1; break into real tasks when Phase 1 ships. Contents: scene-stepped animated artifacts (Tier 3, §8); Codex adapter (proving the §5.5 abstraction, G6); ⌘K quick switcher + full-text search (SQLite FTS5); artifact version diffing (FR-2.4 nice-to-have); export/share bundles; global summon hotkey + menu-bar mode (ActivationPolicy::Accessory); vendored Tier-2 libs for fully-offline viewing; markmap/KaTeX polish; comment threading (replies to answers).","acceptance_criteria":"Decompose into individual issues at Phase 1 completion.","status":"open","priority":4,"issue_type":"epic","owner":"cdcmeehan@gmail.com","created_at":"2026-07-02T05:39:47Z","created_by":"Chris","updated_at":"2026-07-02T05:39:47Z","labels":["backlog","phase2"],"dependencies":[{"issue_id":"conceptify-qmy","depends_on_id":"conceptify-959","type":"blocks","created_at":"2026-07-01T22:40:44Z","created_by":"Chris","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
